# 2026-06-30

## CI/CD pipeline authored — verify-on-PR + GHCR build + Watchtower pull-deploy

Built out the TODO "CI/CD Pipeline" item (don't manually pull/rebuild on every push). Split into two decoupled halves. Files authored (uncommitted in working tree — PR-gated flow):

- **CI half (live-ready):** `.github/workflows/ci.yml` — runs `npm run verify` (build→typecheck→lint→test) on every PR + push to `main`. Read-only token; safe on untrusted fork PRs (no secrets, no write, no deploy path). Optional follow-up: wire `verify` as a required status check (exact `gh api` cmd in the file header; keep `enforce_admins:false`+`approvals:1` from Sortie Step 2, just ADD the check — defer until job name proven stable).
- **CD cloud half:** `.github/workflows/deploy.yml` — on push to `main` (post-merge only, never PR head code), builds `docker/Dockerfile`, pushes `ghcr.io/scolacur/personal-dashboard:latest` + `:<sha>` via GITHUB_TOKEN (packages:write, no PAT). GHA layer cache.
- **CD NAS half:** `docker/docker-compose.nas.yml` (pulls image, no build, + Watchtower sidecar watching only the labelled `app` so it never touches Sortie) and `ops/deploy/README.md` (the 🧑 NAS runbook).

**Key decision — pull-based deploy, NOT self-hosted runner.** NAS is behind home LAN/NAT (no public ingress) AND CPU-weak. Rejected: poll+build on NAS (rebuilds on weak CPU — the TODO RAM/CPU risk), self-hosted Actions runner (untrusted fork-PR code on a *public* repo = hazard). Chose **GHCR + Watchtower pull**: build in free cloud runners, NAS only pulls a finished image. No inbound networking, no untrusted code on NAS.

**Cost = $0 as designed.** Public repo → GHCR public package (unlimited free storage/bandwidth, exempt from the 500MB/1GB free-tier limits that only bill private packages) + Actions minutes unlimited free for public repos. Watchtower is free OSS, self-hosted. Private-package path documented in runbook as fallback.

**Prerequisite flagged:** the app has NEVER been deployed to the NAS (only Sortie has). Watchtower auto-*updates* a running container — runbook Steps 1-4 ARE the first manual app deploy (~1-2 hr, 🧑 Steve-only, NAS/DSM access); every merge after that is hands-off. All YAML validated (ruby psych). Ties to [[2026-06-28]] Sortie deploy precedent (`ops/sortie/README.md` base dir `/volume1/docker/personal-dashboard/`).

## CI/CD DEPLOYED + PROVEN end-to-end — app is LIVE on the NAS

Drove the full first deploy with Steve (SSH/CLI). The hands-off loop is proven: merge → CI verify → deploy.yml builds+pushes GHCR `:latest` (new digest) → Watchtower pulls within ~5 min → `/api/health` shows the new build's SHA. App container `docker-app-1` Up, `docker-watchtower-1` Up (healthy). PRs #9–#15 all merged.

**The deploy automation immediately earned its keep** — building the image for the first time surfaced a chain of latent bugs that `npm run verify` (which never builds the image) and "the app was never deployed" had hidden:
1. **#10** `apps/web` `prepare: svelte-kit sync` broke prod `--omit=dev` installs (svelte-kit is a devDep) → guarded with `|| true`.
2. **#11** Dockerfile build stage was `node:20-alpine` (musl) but better-sqlite3 ships no musl prebuild + runtime `--ignore-scripts` skipped its binary install.
3. **#11** even on glibc, no prebuilt better-sqlite3 matched the ABI → final fix: **compile in build stage** (add python3/make/g++ toolchain), `npm prune --omit=dev`, **copy the pruned node_modules into runtime** (runtime carries no toolchain, runs no install).
4. **#12** nas compose used `./data`/`./.env` (untracked) → match dev compose's gitignored `../data`/`../.env`.
5. **#13** host **8080 is owned by the gluetun VPN container** → remap app to **`8088:8080`** (LAN URL `http://<nas>:8088`).
6. **#14** DJ library mount path was wrong.

**Durable NAS environment facts (non-obvious; verify before assuming):**
- NAS uses **`docker-compose` v1 (hyphenated)**, NOT the `docker compose` v2 plugin. Run from repo root: `sudo docker-compose -f docker/docker-compose.nas.yml up -d`.
- App published on **host port 8088** (8080 = gluetun). Internal port still 8080.
- DJ library rsync lands at **`/volume1/music/dj-library/tracks`** (mounted `:ro` as `/library`), NOT `dj-library-mirror`. Fixed in both compose files + PROJECT.md §4.
- **GHCR package is public** (repo is public) → NAS pulls with no auth.
- Repo clone on NAS: `/volume1/docker/personal-dashboard/personal-dashboard/`; gitignored `data/` + `.env` at its root.
- Compose changes (port, paths, env) need a **manual `git pull` + `up -d`** on the NAS — Watchtower only auto-updates the *image*, never the compose file.

**Version stamp (#15):** deploy.yml passes git short SHA as `APP_VERSION` build-arg → Dockerfile bakes it into a final `ENV` layer (placed last so only that tiny layer changes per commit, which also moves the digest = Watchtower trigger) → `/api/health` returns `{ok, version}` (`dev` locally). Permanent "which build is live" check.

**Open follow-ups:** ~~(a) wire `verify` as a required status check~~ **DONE** — `verify` enforced on `main` (`contexts:["verify"]`, `strict:false`), Sortie Step 2 settings preserved (`enforce_admins:false`, `approvals:1`, check ADDED not replaced). ~~(b) add "ensure library mount path exists" to runbook prep~~ **DONE** — present at `ops/deploy/README.md:84` (`ls -d /volume1/music/dj-library/tracks`). ~~(c) GHA action-runtime Node 20 deprecation warnings~~ **DONE** — actions bumped to current majors (`checkout@v7`, `setup-node@v6`, `login-action@v4`, `setup-buildx-action@v4`, `build-push-action@v7`); no more Node 20 deprecation warnings. **All three 06-30 CI/CD follow-ups now closed.** Session memory still uncommitted per [[2026-06-28]] PR-gated flow.

## Sortie exit-128 stuck-loop — ROOT CAUSE FOUND + FIXED (the #6/#8 "clone failure")

Steve flagged Sortie felt stuck. Diagnosed from GitHub + NAS logs: **#8 parked in `sortie:in-progress` ~14.5h** (frozen since 02:36, no branch/PR/comment), **#6 actively looping** — NAS logs showed `after_create hook failed, rolling back workspace ... exit_code=128`, retrying on doubling backoff (10s→20s→40s, `next_attempt` incrementing).

**Root cause (memory's old hypothesis confirmed):** the `after_create` hook (`ops/sortie/WORKFLOW.md`) did a bare `git clone ... "$SORTIE_WORKSPACE"`. The workspaces volume is **persistent** and dir is named by issue id (`/volume1/docker/personal-dashboard/workspaces/<id>`). On ANY re-dispatch (conflict re-work, re-queue, retry) the dir already exists non-empty from the prior run; Sortie's "rollback" does NOT `rm` it → `git clone` into a non-empty dir = `fatal: destination already exists` = **exit 128**, looping on the worker's prep-retry backoff forever. NOTE: this loop burns **NAS CPU (git clones), NOT Claude quota** — the agent session never starts (workspace prep fails first). `max_sessions:3` caps agent sessions, not workspace-prep retries, so it does NOT bound this loop.

**Fix part 1 (committed `669d36b`, pushed direct to `main` via admin bypass — `enforce_admins:false` lets admin push past the `verify`+PR rules):** `after_create` now `rm -rf "$SORTIE_WORKSPACE"` before cloning. Safe — workspace is disposable, origin is source of truth, `before_run` re-fetches `sortie/<id>` from origin each attempt. ⚠️ NECESSARY BUT NOT SUFFICIENT — a SECOND exit-128 cause (hook proxy) surfaced after the cap reset; see "exit-128 SECOND cause" below.

**Sortie config deploy path is NOT the Watchtower/image flow** — `WORKFLOW.md` is **bind-mounted `:ro`** into the container (`docker-compose.egress.yml:49` / `docker-compose.yml:28`). To deploy a hook change: NAS `git pull` + re-apply compose. ⚠️ **USE THE EGRESS COMPOSE, NOT `docker-compose.yml`** — the live deployed stack is `docker-compose.egress.yml` (squid sidecar `sortie-proxy` on `egress_out`+`egress_internal`; `sortie` on `egress_internal` ONLY = no direct internet, routes via `HTTPS_PROXY=http://egress-proxy:3128`). I mistakenly told Steve to run `docker-compose.yml up -d sortie` (plain non-egress file) → compose recreated `sortie` on the default bridge with **full egress + no proxy = containment DOWN**, and warned `sortie-proxy` was an orphan. **NEVER `--remove-orphans`** (deletes the squid proxy). Correct cmd: `sudo docker-compose -f ops/sortie/docker-compose.egress.yml up -d` (hyphenated v1; ignore the `docker compose` v2 hint in the file header).

🧑 Steve ran the immediate unblock (`rm -rf workspaces/6 workspaces/8`), then recreated both containers via the egress compose — **containment RE-VERIFIED restored** (direct egress=000/BLOCKED, proxied github=200, `HTTPS_PROXY` set) and WORKFLOW.md fix now live. Then re-queued **#8 → `sortie:queued`**; Tank re-queued **#6 → `sortie:queued`** (it was orphaned in `in-progress` — see below). Both should now dispatch clean.

**Third silent-park mode discovered:** container **restart orphans an `in-progress` issue** — the in-memory worker dies with the container, but the issue stays labeled `in-progress`, which is NOT in the dispatch (`queued`) set → nothing re-picks it. So an `in-progress` issue with no live worker + no open PR is invisibly parked. Cure: flip to `sortie:queued`. (Modes 1–3: prep-failure loop, `max_sessions` exhaustion, restart-orphan.)

## Sortie observability/comms design — `sortie:stuck` + `ask_human` (DECIDED, GitHub-first)

Designed the fix for Sortie's core gap: **it can't talk to Steve**, so failures silently park (3 modes above; #8 sat invisible 14h). Two layers:

**Layer 1 — Escalation (one-way, Sortie→Steve).** New terminal label **`sortie:stuck`** (chosen over `needs-human` — too close to the less-urgent `awaiting-human`). A **`sortie-watchdog` scheduled GitHub Action** (in the spirit of the existing `sortie-conflict-rework` Action) finds issues `in-progress` with no open `sortie/*` PR and **no timeline activity >2h** → flips to `sortie:stuck`, comments the reason/last-error, **@mentions Steve**. State-based + external = catches all 3 park modes (hook-based `after_run` would only catch exhaustion, never prep-failure/orphan). Notify: GitHub @mention **forwarded to Discord via a webhook** (Discord's native GitHub webhook endpoint = webhook URL + `/github`, no bot needed). 2h threshold assumes TODOs get decomposed into atomic issues (small PRs).

**Layer 2 — `ask_human` (two-way, async/turn-based).** Agent ends turn with a question as a GitHub comment + sets **`sortie:awaiting-human`** (intentional park, not failure); Discord webhook forwards it. Steve replies → a bridge detects a **human reply on an `awaiting-human` issue** → injects answer into next turn → re-queues via Sortie's **continuation dispatch** (same workspace+branch, already exists). Motivating use case: the **egress allowlist** (`ask_human("allowlist X? once/permanent/no")` → reply → bridge edits `squid.conf` + reloads proxy). 

**Discord direction caveat:** bot→Steve is a one-way webhook (easy). Steve→bot (Discord message → GitHub comment the bot reads) **needs a real Discord bot with message-read scope**, NOT a webhook — that's the only meaningful new infra, which is why **GitHub-first** (reply directly on the issue) is the agreed sequence; Discord is a fast-follow. Build order: (1) watchdog+`sortie:stuck`+@mention, (2) Discord webhook forward, (3) `ask_human` on GH comments, (4) Discord inbound bot. Ties to TODO items: ask_human, Discord, retry-cap, egress-allowlist permission prompt.

## Sortie watchdog + ask_human — BUILT in-session (NOT yet deployed; no Sortie dispatch used)

Steve chose generic free-text `ask_human` for now (allowlist deferred → future agent-dashboard feature: blocked-request log grouped by domain + one-click allowlist button, which belongs on the NAS network where it can reach `squid.conf`, NOT a cloud Action; agent must never self-allowlist). Decided to **keep `sortie:stuck` and `sortie:needs-human` SEPARATE** — do NOT overwrite the native `reactions` review-cap escalation. Built it all this session, no GitHub issues, no Sortie runs (Steve wants Sortie kinks ironed before queuing more work).

**Label taxonomy (3 distinct human states):** `needs-human` (native: review-feedback continuation cap exhausted) · `stuck` (watchdog: in-progress/queued stale >2h) · `awaiting-human` (ask_human: deliberate question, waiting). Created `sortie:stuck` (red b60205) + `sortie:awaiting-human` (yellow fbca04) via `gh label create`.

**Artifacts (all validated; uncommitted in working tree):**
- `.github/workflows/sortie-watchdog.yml` — Layer 1. `cron */15` + `workflow_dispatch` (inputs `threshold_minutes` override, `dry_run`). **Per-state thresholds: queued=20m, in-progress=120m** (Steve's choice). Clock = last `labeled <state>` timeline event, so the timer **restarts on re-entry** (in-progress→awaiting-human→in-progress measures from latest re-entry). Flips stale → `sortie:stuck` + `@scolacur` comment. Leaves `awaiting-human` + terminals untouched. Mirrors conflict-rework's auth model + single-flight concurrency. Detection logic dry-run-verified against live state (no false-positives: #6/#8 read 28/40m = ok). NOTE: uses GNU `date -d` (fine on ubuntu-latest; my local macOS BSD date can't run it).
- `.github/workflows/sortie-ask-human.yml` — Layer 2 inbound. `on: issue_comment`; guard `comment.user.login=='scolacur' && contains(labels,'sortie:awaiting-human')` → flip `awaiting-human → queued`. Bot/github-actions comments don't match (author≠owner) so no loop.
- `ops/sortie/WORKFLOW.md` prompt body — 2 sections added: "check for answers to a question you asked" (run `gh issue view N --comments` first, since issue comments are NOT auto-injected into agent context — only `.issue.description` + PR `.review_comments` are) and "ask_human" how-to (post `### ❓ ask_human` comment, leave tree clean, self-relabel `in-progress→awaiting-human`, end turn). Both `export GH_TOKEN="$SORTIE_GITHUB_TOKEN"` before gh.
- `ops/sortie/README.md` — new "Observability & comms" section documenting both layers + deploy/live-verify checklist.

**DEPLOY (pending):** (a) **merge the 2 workflow files to `main`** — Actions only run from the default branch's own copy (cron + issue_comment both need this); (b) **recreate the NAS container via the EGRESS compose** so the updated WORKFLOW.md prompt mounts. **LIVE-VERIFY (needs a Sortie run, deferred):** agent can run `gh` mid-turn (is `SORTIE_GITHUB_TOKEN` in the *agent* env, not just hooks?); self-relabel to `awaiting-human` isn't fought by the reconciler; on re-queue the agent reads the reply + continues vs restarts. Then set up repo→Discord webhook (URL + `/github`) for mention/question forwarding.

**FLAG resolved → root cause = max_sessions cap.** #6/#8 sat in `sortie:queued` undispatched. Logs showed `tick completed candidates:2 dispatched:0 running:0 retrying:0` repeatedly = Sortie healthy & polling, sees both, slot free, but REFUSES to dispatch. **That signature = per-issue `max_sessions:3` exhaustion** (Sortie counts `run_history` rows per `issue_id` in `.sortie.db`). Confirmed via DB: **#6=3 sessions, #8=31 sessions, all `failed`** (today's exit-128 storm wrote 31 rows for #8). 

**The quota-refund janitor (`quota-refund.sh`) deliberately will NOT fix these** — it only deletes rows matching the quota-fail signature (`status='failed' AND error LIKE '%turn_failed: success%' AND total_tokens=0`); #6/#8 are REAL failures (git-clone exit-128), so its classifier excludes them (script names #8 explicitly). Real-failure caps require the **manual reset**: stop container → backup `.sortie.db` → `DELETE FROM run_history; DELETE FROM session_metadata WHERE issue_id IN (...)` → restart. Now SAFE because the exit-128 root cause is fixed + workspaces cleared, so re-dispatch clones clean. Schema: `run_history(issue_id,status,error,total_tokens)` + `session_metadata(issue_id)`. NAS host has NO `sqlite3` → use an **official** image: `docker run --rm -v /volume1/docker/personal-dashboard/data:/data alpine sh -c "apk add --no-cache -q sqlite && sqlite3 /data/.sortie.db \"DELETE FROM run_history WHERE issue_id IN ('6','8'); DELETE FROM session_metadata WHERE issue_id IN ('6','8');\""` (container STOPPED first; string literals need SINGLE quotes — double quotes = SQLite column name error). DB at `/volume1/docker/personal-dashboard/data/.sortie.db`. (Initially used `keinos/sqlite3` then switched to official `alpine` per [[prefer-official-docker-images]] — see PREFERENCE below.)

**Insight — the watchdog + cap form a coherent loop:** a capped issue stays labeled `queued` and never dispatches; the new 20m queued-stale watchdog will flip it to `sortie:stuck` + @mention — i.e. capped issues become VISIBLE for a human decision (manual-reset to retry, or wontfix) instead of silently sitting. This is the intended Layer-1 behavior. Ties to TODO "Retry cap / max-attempts".

## PREFERENCE (Steve feedback): official Docker images only when touching his data/infra

When suggesting a command that pulls a Docker image (or introduces tooling) to operate on Steve's data/infrastructure, **default to Docker Official Images** (`alpine`, `python`, etc.) or tools already on the box — **never an unvetted third-party/community image without flagging it explicitly**. Steve immediately challenged `keinos/sqlite3` ("this is my first time seeing keinos") when I suggested it to edit `.sortie.db`. **Why:** he runs a security-conscious setup (built the egress-hardened squid proxy specifically to contain token exfil) — pulling a random community image and bind-mounting his DB into it is exactly the supply-chain risk he guards against. **How to apply:** prefer `alpine` (+`apk add <pkg>`) or an official language image with the tool in stdlib (`python:3-alpine` has sqlite3); if a 3rd-party image is truly the only option, state the exposure (read/write scope, network) before recommending; prefer read-only mounts. (Cross-project preference, but recorded here per this project's golden rule that memory stays in-project.)

## Sortie exit-128 SECOND cause — hook proxy (FIXED `3f07151`)

After the cap reset, #6/#8 dispatched but exit-128 RETURNED. Root cause #2: under the egress-hardened deploy `sortie` is on `egress_internal` (NO direct internet — only route out is the squid sidecar `egress-proxy:3128`), and **Sortie runs hooks in a restricted env where `export http(s)_proxy=...` does NOT take effect for git** (an inherited uppercase `HTTPS_PROXY` wins, or the export isn't applied) → git clone/fetch/push hang → exit 128. **Proven:** a minimal env (`env -i ... https_proxy=...`) clones fine, so the env is capable; the hook's `export` just wasn't being honored. **Fix:** pass the proxy DIRECTLY on each network git command via `git -c http.proxy=http://egress-proxy:3128` (git config overrides ALL proxy env vars; `http.proxy` covers https remotes too), and inline `HTTPS_PROXY/HTTP_PROXY` + `GH_TOKEN` on each `gh` call in `after_run`. Applied to after_create (clone), before_run (fetch/ls-remote), after_run (push + gh). The earlier `export`-based attempt (`55c56cf`) was insufficient. **Confirmed working: #6 ran clone→agent→PR end-to-end → PR #17.** (The AGENT's own gh calls in the ask_human prompt are fine — the agent process has the proxy since it needs it for Claude; only HOOKS get the stripped env.)

## 🔴 P1 Sortie bug — handoff/`after_run` killed by `context canceled` (logged in TODO)

On the agent's `needs-human-review` exit, Sortie cancels the worker context, which races and kills BOTH the `after_run` hook mid-execution AND the handoff label transition. Log (#6, 18:44): `agent signaled status ... needs-human-review` → `running hook after_run` → `handoff transition failed, releasing claim ... handoff_state:sortie:in-review, error:context canceled`. Result: PR #17 created (gh pr create ran early in after_run) but `after_run` died BEFORE writing `.sortie/scm.json` (**confirmed missing**), and the issue landed with NO label (Sortie removed `in-progress`, couldn't add `in-review`). **Impact:** successful runs land label-less (invisible to both the review reactions AND the watchdog, which only watches in-progress/queued) and have no `scm.json` (so `review_comments`/`ci_failure` reactions can't locate the PR → review/handoff loop broken). `after_run` is fundamentally unreliable — it runs AFTER the agent exits under a tearing-down context. **Fix (next session, TODO PRIORITY 1):** move `git push` + `gh pr create` + `scm.json` write INTO the agent's turn (stable context, full env incl. proxy); reduce `after_run` to a safety-net. Tank manually set #6 → `sortie:in-review` as a stopgap (label only; scm.json still missing so reactions on #17 won't fire).

## SESSION END STATE (Sortie)

- **Committed to `main`:** `669d36b` (after_create rm -rf), `0115850` (watchdog + ask_human Actions + WORKFLOW.md ask_human prompt + README), `3f07151` (inline proxy in all 3 hooks). This memory + TODO committed separately.
- **#6:** SUCCESS → PR #17 ([UI] reusable Widget + 3D flip, +150/-27, 5 files). Manually labeled `sortie:in-review`. Steve to review/test the PR.
- **#8:** kept exit-128'ing AFTER all fixes were committed — ROOT CAUSE was the **single-file-mount inode trap**: the container ran the OLD (broken) hook because `git pull`+`up -d` never re-resolved the mount. Once deployed via `--force-recreate` (`sortie-refresh`) with the cap cleared, **#8 dispatched and ran successfully** (agent active, clone clean, no 128). The proxy/idempotency fixes were correct all along — just not actually deployed until force-recreate. Debug logging was added then reverted (`911ee78`); the NAS still runs the debug hook until the next `sortie-refresh` (harmless; do it AFTER #8 finishes so as not to kill the live run). **#8 → PR #18** ("sortie: resolve #8", Pomodoro, +284/-1, 6 files) — hit the P1 handoff bug (PR made but label not auto-set → invisible until Steve found it; now manually `sortie:in-review`). **Both #6 (PR #17, merged) and #8 (PR #18) succeeded — full pipeline proven.**
- **Watchdog + ask_human:** code live on `main`; ask_human round-trip + watchdog cron still need live verification (deferred). Labels `sortie:stuck`/`sortie:awaiting-human` created.
- **Next session:** the P1 after_run→agent-turn fix.

## Sortie deploy gotcha + `sortie-refresh` alias (IMPORTANT)

**Single-file bind mount + `git pull` = stale container until RECREATE.** `docker-compose.egress.yml` mounts WORKFLOW.md as a single FILE (`...:/home/sortie/WORKFLOW.md:ro`). `git pull` replaces the file with a NEW inode, but the container keeps pointing at the OLD inode — `docker restart` / plain `up -d` do NOT re-resolve it. Only **`--force-recreate`** (or stop+rm+up) re-resolves the mount. This silently caused several "the fix didn't take" rounds tonight (container ran the pre-edit hook). Verify a deploy landed with `docker exec sortie grep -c <marker> /home/sortie/WORKFLOW.md`. (Long-term: bind-mount the DIRECTORY `ops/sortie/` instead of the file.)

**Steve has a `sortie-refresh` shell alias** = `sudo git -C /volume1/docker/personal-dashboard/personal-dashboard pull origin main && sudo docker-compose -f .../ops/sortie/docker-compose.egress.yml up -d --force-recreate`. Does pull + force-recreate (handles the inode gotcha). **Going forward: just tell Steve to run `sortie-refresh` to deploy a WORKFLOW.md / compose change.** (Does NOT clear `.sortie.db` caps — still separate.)

## P1 FIX BUILT — Sortie hand-off moved into the agent turn (uncommitted, awaiting Steve)

Picked up the explicit "next session" PRIORITY 1 from this morning. Root cause confirmed from the lifecycle (`clone → branch → agent works → self-review → after_run [commit/push/PR/scm.json]`): on a `needs-human-review` exit Sortie cancels the worker context, which **races and kills BOTH `after_run` AND its `handoff_state` label transition** (`context canceled`) → PR made but `scm.json` missing + issue label-less + invisible to reactions/watchdog. Logged as **D-016**.

**Fix (uncommitted in working tree — Steve to review/commit per PR-gated flow):**
- **`WORKFLOW.md` prompt body** — new "Finish" section: the AGENT does verify → commit → push → `gh pr create` → write `.sortie/scm.json` → relabel `in-progress→in-review` **in its own turn** (stable context + full env). **Ordering is load-bearing: the relabel MUST be last** — `in-review` is not in `active_states`, so applying it earlier can make the reconciler cancel the worker mid-turn (the exact bug). Proxy passed inline (`-c http.proxy` / `HTTPS_PROXY`), mirrors the hook + ask_human patterns.
- **`WORKFLOW.md` `after_run`** — demoted to an idempotent **safety-net**: commits stragglers, then if `local HEAD == remote` AND `scm.json` exists → no-op exit 0; else completes push/PR/scm.json. No longer relabels (agent + rescue-job own that).
- **`WORKFLOW.md` `before_run`** — now **regenerates `.sortie/scm.json`** on the follow-up (existing-branch) path. Answers the open scm.json question: `after_create`'s `rm -rf` wipes the loose (uncommitted) scm.json each dispatch; regenerating in `before_run` makes it survive **regardless of whether Sortie reads it pre-wipe (persistent workspace) or post-clone (fresh workspace)** — that read-timing is unconfirmed against this Sortie version.
- **`.github/workflows/sortie-watchdog.yml`** — new **`rescue-labels`** job (+`pull-requests: read`): any open `sortie/*` PR whose issue has no `sortie:*` state label → set `sortie:in-review` + comment. Belt-and-suspenders with the agent self-relabel (**Steve chose "both"**). Idempotent; leaves any already-labeled issue alone. Both YAML files validated (front-matter + workflow parse clean).
- Docs: **D-016** added to DECISIONS.md; README Architecture + Observability sections updated (also corrected the watchdog schedule doc: it's `*/15`, README had said 30m).

**CONFIRM-on-first-live-run (can't verify locally — Sortie isn't installed on this box, no source/docs):** (1) the agent turn's env actually carries the proxy + `SORTIE_GITHUB_TOKEN` for `git`/`gh`; (2) an agent self-relabel to `in-review` isn't fought by the reconciler / `handoff_state`; (3) actual `scm.json` read timing. **Deploy = `sortie-refresh`** (force-recreate; inode trap). Then watch a real run: agent pushes + opens PR + sets in-review all in-turn, `after_run` logs the no-op, issue lands `sortie:in-review` with `scm.json` present.

Also reconciled the morning's stale TODO/memory: `verify` IS now a required status check (✅), theming work is committed on `main`, #8→PR #18 open (`in-review`). Marked the closed CI/CD + exit-128 + watchdog items in TODO.md "Shipped 2026-06-30".