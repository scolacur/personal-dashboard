# 2026-05-26

## Session start — matrix-session-start

PROJECT.md loaded (monorepo scaffold + Music Tracker matcher steps 1–4 done per last commit). Memory loaded from 2026-05-25 and 2026-05-24.

Key context from recent sessions:
- Symphony is a standalone project in `multi-agent-linear-workflow/` (D-013)
- Claude Code (`claude --print`) is the agent runner (D-013)
- Mission Control UI lives in Personal Dashboard, calls Symphony's `/api/v1/state` API (D-014)
- SQLite for now; PostgreSQL flagged as upgrade path for `agent_*` tables when concurrent writes become a bottleneck (D-010)

Active memory target for this session: `/Users/steve/Documents/Dev/projects/personal-dashboard/MEMORY/`
