# 2026-06-28

## Session start — core-session-start

Project session bootstrapped. PROJECT.md loaded (monorepo: Fastify + SvelteKit + SQLite; Music Tracker matcher steps 1–4 done). Memory loaded from MEMORY.md index plus yesterday's daily file (2026-06-27).

Background decisions to keep in mind (not in-flight tasks):

- Symphony is a _sibling_ standalone project in `multi-agent-linear-workflow/`, runner is Claude Code `claude --print` (D-013).
- This Dashboard hosts a thin Mission Control UI that only consumes Symphony's `/api/v1/state` API; it owns no agent data (D-014).
- SQLite for now; PostgreSQL flagged as upgrade path for `agent_*` tables (D-010).

Active memory target for this session: `/Users/steve/Documents/Dev/Projects/personal-dashboard/MEMORY/`

## Sortie-readiness — closed the mechanized-standards GAP (#3)

Continuing the Architect's Sortie-readiness audit (`core/Agents/Harness Engineer/SORTIE_READINESS.md`, 2026-06-28). Dashboard floor #1–5 were already done; #6–7 are Sortie-gated (NAS deploy). Picked off the biggest non-gated gap: **#3 Mechanized standards** (was "no ESLint/Prettier, no config of any kind").

Done this session:

- Added flat ESLint config (`eslint.config.mjs`) covering all 3 workspaces — ESLint 9 + typescript-eslint 8 + eslint-plugin-svelte 3, non-type-aware (fast, no project-refs). + `.prettierrc` / `.prettierignore` (Prettier 3 + prettier-plugin-svelte).
- Root scripts: `lint` (`eslint .`), `format`, `format:check`. **Wired `lint` into `verify`** → `build && typecheck && lint && test:matcher`.
- Fixed the 5 real errors lint surfaced: ternary-as-statement in `library.ts`, useless regex escape in `normalize.ts` (semantics preserved, matcher still 6/6), missing `{#each}` key in `+page.svelte`. Disabled `svelte/no-navigation-without-resolve` (static adapter, no `base`, LAN-only — justified inline).
- Cold `verify` (all build outputs deleted) EXIT 0 in ~10.3s. Still well under the 60s floor.

Not done / deliberately deferred:

- `format:check` NOT gated in verify (avoids a whole-repo reformat churn); `npm run format` is opt-in. Decide later whether to normalize + gate.

## Sortie-readiness — closed the test-depth gap (audit's "highest-value follow-up")

Added a real test framework and deepened coverage from 1 hand-rolled fixture (6 assertions) to **41 vitest tests**.

Done this session:

- Installed **vitest 3** in `apps/server`; `apps/server/vitest.config.ts` (include `src/**/*.spec.ts` — convention is `.spec.ts`).
- `normalize.spec.ts` (30 tests) — diacritics fold, feat./ft/featuring extraction, remix-keyword extraction (remix/mix/edit/version/dub/vip/bootleg/rework), feat+remix combined, collab-separator normalization (x / vs / vs. / with / & / comma), alphabetical artist sorting, filename-noise stripping (extension, leading track number, trailing catalog), the "1999 is not a track number" edge, whitespace squeeze.
- `matcher.spec.ts` (11 tests) — migrated the fixture library + cases into proper `describe/it/expect`; added empty-library, min-confidence floor + descending-sort invariant, and `shouldAutoConfirm` unit tests.
- Scripts: `apps/server` `test:matcher` → **`test` (`vitest run`)**; root `test` = `npm run test -w apps/server`; **`verify` now runs `build && typecheck && lint && test`**. **Deleted `matcher.fixture.ts`** — one test system now.
- Cold `verify` EXIT 0 in ~11.4s, 41/41 green. All expected values were correct on first run.

Still open: pilot ticket (#8); `format:check` gating decision. #6–7 remain Sortie/NAS-gated.

**Cross-repo follow-up:** Steve granted one-session permission to edit core's `SORTIE_READINESS.md` (outside the project dir). **Done** — updated §2 rows #1 (Verifiable) + #3 (Mechanized standards) to Met, the "#1 detail" section marked SUPERSEDED, Go/No-Go #5 deepened, and the verdict caveat updated (shallow-verify caveat retired; noted coverage is scoped to matcher/normalize — routes/scanner/web still untested). Still TODO in a CORE session: META-TODOS doesn't reference #3 specifically, so no edit needed there; the doc was the load-bearing artifact.
