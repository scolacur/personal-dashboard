# 2026-09-11

## 10:39 — A connect timeout to the NAS is not evidence about off-LAN access

Bootstrapped via `/harness pd`. The board was unreachable at session start (curl exit 28,
`http=000`) and I attributed it to the off-LAN problem **PD-698 ([Mac Mini Migration] Re-establish
off-LAN access to the board — and raise the bar to remote dev work)** exists to fix. Wrong: it was
Steve's wifi, and the same URL returned 200 once it cleared.

Worth keeping because the two are indistinguishable from curl's side. **Rule out the LAN link before
reaching for the off-LAN story** — a timeout to `192.168.68.50:8088` carries no information about
either PD-698 or PD-34. The rest of the board was intact: 785 tickets, highest PD-698, matching
2026-09-06's 779/PD-692 plus what was filed since.

## 11:20 — Four sessions of uncommitted memory audited against the code, then landed

Committed as **`12a7e75`** (unpushed): the untracked 09-03 and 09-06 day files, the 09-02 file's
folded second session, the aging pass that moved 2026-08-21/22/23 into `archive/`, and three unfolded
session inboxes.

Supersession was cheap to rule out: **no code has been committed since `eae3ac0`** (PD-611, #389,
2026-09-02), and local `main` is level with `origin/main`. Everything checked out against the tree —
PD-650's route/store claims, the soft-delete archiving mechanics, D-080's `EPIC_REQUIRED` refusal,
D-089's `refine_stale`, every board id and parent-Epic, and 09-06's coverage counts (106 components,
0 component tests, 0 legacy `export let` — still exact).

**One real defect.** `MEMORY.md` claimed "`refined` becomes a hard queue gate" beside "PD-611 shipped
(#389)", making a *decision* read as *shipped code*. It has not shipped: `robotQueueCandidates`
(`agent-worker/src/jobs/robot/select.ts:76`) still gates on `ready = 1 OR ready_bypassed = 1`, and
**PD-632 ([Board] Gate the Queue on refinement: one predicate, enforced)** is still backlog. Index
line rewritten; the 09-02 day file's two present-tense assertions moved to the conditional its own
PD-612 line already used.

**The lesson is about the index, not the day file.** The day file's heading was honest ("five
decisions, three tickets"). The damage happened in the one-line summary, where "decided and filed"
compressed to "becomes" — next to a genuine "shipped (#389)" that lent it false authority. The index
is the lossiest thing in the memory system **and the only part always loaded**, so dropping "not yet
built" is the most expensive compression available.

## 11:50 — PD-34 is genuinely done; it was a logged-out Tailscale client all along

**PD-34 ([Infra] HIGH — Reverse proxy for off-LAN access.)** is correctly `completed`. Verified from
the laptop over the tailnet: `http://100.82.231.58:8088` and `http://stevenas:8088` both return 200,
and the route is **`utun7`**, not the LAN interface. Of the two readings 2026-09-06 posed, **reading
one was right**. Everything PD-34 owed exists — [[D-030]] and the runbook `ops/access/README.md`,
committed in `e2b1b8b` (2026-07-01 21:34), with the ticket hand-ticked 83 minutes later. Its missing
`completedAt`/`agentState` only means it predates the Robot loop.

**The wrong turn, because its shape will recur.** I first concluded the opposite and wrote it up with
confidence, off four corroborating-looking facts: Synology runs `tailscaled --tun=userspace-networking`,
there is no `tailscale0` interface, the tailnet IP is on no host interface, and `tailscale serve
status` says "No serve config". The clincher was **`curl` to the NAS's own tailnet IP, from the NAS** —
the one worthless test in the set, because **a node cannot reach its own tailnet address without a
TUN**. Userspace-networking *does* proxy inbound tailnet connections to local host ports; that is why
the Synology package runs that way. Worse: **I had flagged that self-directed curl as unreliable one
step earlier and then let the corroboration override my own caveat.** The correct test — curl it from
a second device — took thirty seconds once tried.

The near-miss: the proposed remedy was `tailscale serve --bg 8088` on prod, which would have appeared
to fix a system that was not broken and permanently muddied PD-34's record. **Nothing was run.**

## 12:10 — PD-698 corrected and returned to backlog

Briefly claimed (answering its opening question is its step 1), then PATCHed back to `backlog` /
unassigned once the answer turned out to be "nothing to do until the Mac Mini move". Body rewritten:

- **Struck, not deleted, the PD-43 ([Dashboard Shell] Auth) prerequisite claim.** [[D-030]] made the
  tailnet the auth boundary deliberately — that is why Tailscale beat a public reverse proxy. This
  correction stands independently of my error above; the old body would have had someone build auth
  first for nothing. Struck-through so the reversal reads as intentional, as PD-611 got on 09-02.
- **Reshaped the "host-specific" justification** from "the move invalidates it, re-establish" to what
  it is: install Tailscale on the Mac Mini and log in.
- Kept the security section — the Firewall reasoning is sound — noting PD-43 becomes a prerequisite
  again *only* if this drifts toward public exposure.
- Collapsed seven verbatim repetitions of PD-34's full title to a short identifying phrase, which is
  what the id+title convention actually asks for.

## 12:35 — C-29 re-filed as PD-699, then archived

**PD-699 ([recurring job] Token monitoring tools landscape research)** (id 861) created as an Epic,
P3, backlog, in the PD project; `duplicates` relation drawn **from C-29 → to PD-699**; C-29 (id 184)
then archived (`DELETE /tickets/184` → 204, no `?cascadeMembers=1` — zero members, checked first).

**Why a duplicate and not a fix in place:** C-29 was *already* in the Personal Dashboard project. Only
its display id was wrong, because ids are minted from the project's `key` at creation and **never
re-issued** — it was created in Core and moved. **There is no edit that corrects a prefix**; re-filing
is the only route. Remember this before anyone tries.

Body copied verbatim but for one repair: it cited `PD-519 ([Epic] Robot loop — model cost & access)`,
while PD-519 was retitled on 09-02 to **[Epic] Switch from Claude to OpenRouter for all Osiris
agents**. A stale title *inside a body* is what the id+title convention exists to catch, and it
survived because nothing re-reads bodies.

## 12:50 — Steve set the Epic order; and the board sorts by priority, not raw sortOrder

Steve re-ordered the Epics to be the order he wants to tackle them in. Top of the P1 band:
PD-493 (Task Tracker board — views & interactions) · PD-465 (Misc Minor Bugfixes) · PD-519 (Switch to
OpenRouter) · PD-609 (Robot loop hardening v2.5) · PD-605 (Ticket & Epic Detail Page Redesign V3) ·
PD-530 (Pomodoro V2) · PD-188 (Migrate to Mac Mini) · PD-350 (Karpathy Memory).

**Correction worth keeping:** I read the order off raw `sortOrder` and reported three P1 Epics —
PD-366 (Recurring Ticket Audit Job), PD-555 (Backup verification), PD-672 (Osiris observability) — as
"stranded below fifty P4 widgets". They are not. The board **groups by priority first, then sort
order**, so Steve correctly sees them at the bottom of the P1 section. A raw `sortOrder` listing does
not reproduce the GUI and must not be presented as "board order". What *is* true: 33 of the 74 active
Epics still have `sortOrder == createdAt`, so **within their priority band** they sit in creation
order rather than a chosen one.

Also confirmed not-a-bug: PD-436, PD-401, PD-382 and PD-556 are 100% complete while their stored
`status` is still `backlog`. `deriveEpicLane` (`store.ts:996`) is server-side and the board renders
the derived lane, so they correctly read Completed. An Epic's stored status is not the thing to check.

## 23:55 — Clearing the open items: the inbox backlog, the runbook, and D-090

Steve asked for the session's open items to be worked rather than carried. All four closed.

**The three orphaned inboxes folded** (`573cd13`), on Steve's go-ahead — the "leave another session's
inbox alone" rule defers to sessions that may still be *open*, and these were 4–9 days dead. 09-02
gained its third concurrent session (Party Garage onto the board as the fourth project; PD-650),
09-06 gained the coverage baseline, and **2026-09-07 was created from scratch** — it had no day file
at all. `MEMORY/runs/` is empty but for its README. The 09-06 index line was also corrected: it still
claimed off-LAN access fails.

**PD-700 ([Infra] Correct the off-LAN runbook: userspace-networking, not the 0.0.0.0 bind)** filed,
built and merged (PR #390), then PATCHed to `completed` by hand — a merged PR does not complete its
ticket. The runbook now explains that inbound reach comes from **tailscaled's userspace proxy, not a
host route**, and carries a Troubleshooting section: never curl a node's own tailnet IP from that
node, and check *logged-out client → LAN fault → NAS state* in that order.

**A dead link found on the way:** the runbook pointed at `D-030-off-lan-access-via-tailscale-with.md`,
but PD-490's one-decision-per-file refactor had renamed the file. A repo-wide sweep found exactly one
other dead `DECISIONS/` link — `MEMORY/archive/2026-08-12.md` cites
`D-076-epic-is-the-unit-of-dispatch.md`, whose content is actually **D-080** while D-076 is now a
different decision entirely. **Left alone deliberately** — an archived day file records what was
believed then, and that citation is an artifact of the retired provisional-id era.

**[[D-090]] written and merged** (PR #391), on Steve's call to author it ahead of the implementation
so PD-694 starts against a settled record. Id allocated from the counter. Its substance: the shared
daily-log store is **§5's documented pattern, not a relaxation of it** — §5 forbids widget-to-widget
imports and routes shared things to `apps/server/src/lib/`, where PD-442 already put `job_runs`. What
is new is that `job_runs` was infrastructure and the daily log is *subject matter* — the first shared
**domain** table. Both alternatives that will be re-proposed are recorded. PD-693's body now cites
D-090 in place of its "probably owes a `D-NNN`" note.

**`HIGHEST_DECISION_AT_SEED` 89 → 90.** `decision-ids.spec.ts` failed until it was raised, which is
the design working — the constant is a checked literal, and every new decision file trips it.

**The killswitch stays off**, Steve's decision: nothing PD-side is queued or robot-assigned, and
PD-632's refinement gate has not shipped, so there is little for the loop to pick up.

## 00:05 — PD-701 filed: the day file should be generated, not written by hand at wrap-up

Steve's intake, and today's own failure is the evidence for it. **PD-701 ([Memory] The day file
becomes a generated artifact — wrap-up emits a session summary, a daily job synthesises the day)**,
under PD-350 ([Epic] Karpathy Memory), **P1 by inheritance**, `ready`, unrefined. `relates` edges to
PD-513, PD-514 and PD-691.

**D-071 named this trigger and named this remedy**: *"if `MEMORY/runs/` is routinely non-empty, which
means wrap-up is not running and the **curation owner needs to change rather than the format**."* It
fired. The tension that makes it self-worsening is worth stating plainly: **a later session cannot
safely fold someone else's inbox** (it might be live), so the backlog can only grow — the safety rule
and the curation rule point opposite ways, and safety always wins.

**The load-bearing half is "whatever merged that day", not the session summaries.** A summary still
depends on the session ending well; the merge record survives a session that crashed or was closed
mid-task. That became the ticket's acceptance test — *a session that never runs `/wrap-up` still has
its merged work appear in that day's file* — which is what separates this from nagging harder about
wrap-up.

## Open

- **Three stale worktrees** from earlier sessions: `pd-471-reddit-client` (on branch
  `memory-bst-handoff`), `pd-498-decision-inbox`, `pd-498-numbering-cycle`. The PD-498 pair is almost
  certainly dead — D-088 *deleted* the inbox and the numbering cycle — but a worktree can belong to a
  live session, so none were removed.
- **Tailnet hygiene:** the laptop is the **seventh** `steves-macbook-pro*` node, the prior six offline
  24–58 days. Each login mints a new one. Worth a prune in the admin console.
- **The Robot dispatch killswitch is still on**, now deliberately. Resuming is two flags,
  `ROBOT_DISPATCH_ENABLED` **and** `EVALUATOR_ENABLED`.
- **PD-698 ([Mac Mini Migration] Re-establish off-LAN access to the board)** is back in `backlog`,
  correctly scoped down to one migration step plus the remote-dev-work requirement.
- Nothing is claimed on the board and nothing is in the Queue.
