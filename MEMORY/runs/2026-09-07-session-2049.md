# 2026-09-07 — session 20:49

## 20:49 — Session initialized

Bootstrapped via `/harness pd`. PROJECT.md loaded (§9 glossary read). Memory aging pass archived
`2026-08-23.md` (past the 14-day window) and rolled its index line into `archive/INDEX.md`. No ticket
named in the argument.

Pre-existing uncommitted memory changes were in the main checkout on arrival (a previous aging pass,
plus two other sessions' inbox files in `MEMORY/runs/`). Left alone — not mine to commit.

## 20:55 — Lookup: the renaming epic

Steve asked what was decided about renaming "Personal Dashboard". Answer: **the naming model is
decided; every mechanical step is still `backlog`, unrefined, unclaimed.**

The epic is **PD-582 (Rename Personal Dashboard to The Construct)** — id 688, P2, backlog, five
members, all `ready=True` but `refined=False`:

- PD-583 (Dev-wide reference sweep — inventory every mention before renaming anything)
- PD-584 (Decide the display-id prefix — recommendation: leave `PD`, rename only the display name)
- PD-585 (Decide whether a board project can span repos — and how cross-repo tickets are handled)
- PD-586 (Execute the rename — repo, folder, package names, docs and glossary)
- PD-587 (Make Osiris the user-facing product/brand name — reconcile with PROJECT.md's internal-only rule)

**The four-layer vocabulary in PD-582's body is the substance of the decision**: the platform
(substrate) / **Osiris** (control plane) / **The Construct** (product) / **Da Steve Zone** (Steve's
instance). It splits three things one label was doing at once. The metaphor is load-bearing rather
than decorative — the Construct is the space you build in, the Osiris is the vessel that loads you
into it, which is the real dependency direction.

**Nothing is written to `DECISIONS/` yet.** The only naming decision on file is
`D-061 (The agent control plane is named Osiris)`, and PD-587 exists specifically to *reverse* part
of it: D-061's scope note and PROJECT.md §9's Osiris `_Avoid_` clause both say the codename stays out
of public-facing copy, and Steve decided on 2026-08-30 he wants Osiris visible as the brand. D-061
also **rejected "Construct"** at the time ("a generic programming noun") — PD-582 picks it anyway for
a different layer, which is coherent but should be said out loud in whatever decision records this,
or it reads as an oversight.

### Two conflicts found that the epic does not name

- **PD-445 (Rename Personal Dashboard repo to Da Steve Zone and make private)** — P3, backlog, under
  PD-454 (Misc refactorings) — is the *older, contradictory* plan. It renames the repo to Da Steve
  Zone and PD-xxx tickets to DSZ-xxx. PD-582 makes Da Steve Zone the *instance* (not the repo) and
  PD-584 recommends keeping the `PD` prefix. PD-445 should be closed as superseded or explicitly
  reconciled; leaving both open means whichever is picked up first wins by accident.
- **PROJECT.md §9 has no entry for The Construct, the platform, or Da Steve Zone.** The 2026-09-02
  file already flagged "Da Steve Zone" as new vocabulary missing from §9. PD-586 carries the glossary
  edit, so it is tracked — but the vocabulary is in a ticket body, which is not where the glossary
  lives.

### Adjacent rename tickets, deliberately outside this epic

PD-649 (Rename Task Tracker to Project Tracker, P1), PD-416 (Dev Ops: rename tickets API endpoint to
`/api/devops/task-tracker/*`, P4, deferred), PD-531 (Site Status: rename the `sortie` wire field),
PD-642 (Cleanup: retire the remaining live `sortie` references), PD-258 (Rename packages folder to
lib). None blocked on PD-582.

**Sequencing, per the epic:** PD-583's sweep runs first and produces the inventory; PD-584 and PD-585
decide; PD-586 executes. Nothing renames until the blast radius is known.
