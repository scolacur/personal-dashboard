# 2026-09-07

*(Folded from `runs/2026-09-07-session-2049.md` at the 2026-09-11 wrap-up — the session ended without
one. A lookup session; nothing was changed.)*

## 20:55 — The rename: the naming model is decided, every mechanical step is not

Steve asked what had been decided about renaming "Personal Dashboard". The epic is **PD-582 ([Epic]
Rename Personal Dashboard to The Construct)** — P2, backlog, five members, all `ready` but **none
refined**, so none of it is queueable.

Members: PD-583 (Dev-wide reference sweep — inventory every mention first) · PD-584 (Decide the
display-id prefix — recommends leaving `PD`, renaming only the display name) · PD-585 (Decide whether
a board project can span repos) · PD-586 (Execute the rename — repo, folder, packages, docs, glossary)
· PD-587 (Make Osiris the user-facing brand name).

**The substance is the four-layer vocabulary in PD-582's body**, which splits three things one label
was doing at once: the platform (substrate) / **Osiris** (control plane) / **The Construct** (product)
/ **Da Steve Zone** (Steve's instance). The metaphor is load-bearing rather than decorative — the
Construct is the space you build in, the Osiris is the vessel that loads you into it, which is the
real dependency direction.

**Nothing is written to `DECISIONS/` yet.** The only naming decision on file is **D-061 (The agent
control plane is named Osiris)**, and **PD-587 exists specifically to reverse part of it**: D-061's
scope note and PROJECT.md §9's Osiris `_Avoid_` clause both keep the codename out of public-facing
copy, and Steve decided on 2026-08-30 he wants Osiris visible as the brand. D-061 also **rejected
"Construct"** at the time as "a generic programming noun" — PD-582 picks it anyway for a *different
layer*, which is coherent but must be said out loud in whatever records this, or it reads as an
oversight.

## 21:05 — Two conflicts the epic does not name

- **PD-445 (Rename Personal Dashboard repo to Da Steve Zone and make private)** — P3, backlog, under
  PD-454 ([Epic] Misc refactorings) — is the **older, contradictory plan**. It renames the repo to Da
  Steve Zone and PD-xxx to DSZ-xxx. PD-582 makes Da Steve Zone the *instance*, not the repo, and
  PD-584 recommends keeping `PD`. **Leaving both open means whichever is picked up first wins by
  accident** — PD-445 should be closed as superseded or explicitly reconciled.
- **PROJECT.md §9 has no entry** for The Construct, the platform, or Da Steve Zone. PD-586 carries the
  glossary edit so it is tracked, but the vocabulary currently lives in a ticket body, which is not
  where the glossary lives.

Adjacent rename tickets, deliberately outside the epic and not blocked on it: PD-649 (Task Tracker →
Project Tracker, P1), PD-416 (Dev Ops endpoint rename, P4, deferred), PD-531 (Site Status `sortie`
wire field), PD-642 (retire remaining live `sortie` references), PD-258 (packages → lib).

**Sequencing per the epic:** PD-583's sweep runs first and produces the inventory; PD-584 and PD-585
decide; PD-586 executes. Nothing renames until the blast radius is known.

## Open

- **Nothing was changed this session** — no edits, no board writes.
- **PD-445 vs PD-582 is unreconciled** and is the live risk.
- Pre-existing uncommitted memory was in the main checkout on arrival and was left alone. (Landed
  later, on 2026-09-11, as `12a7e75`.)
