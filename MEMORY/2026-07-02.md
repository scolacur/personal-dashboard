# 2026-07-02

## 01:19 — Mac Mini migration design finalized (D-034) + board reconciled; preview trio retired

Continuation of the 2026-07-01 evening `/grill-with-docs` (ran past midnight). The original `/to-sortie-issues PD-15/16/44` request had pivoted into designing the **Mac Mini M4 migration**.

**Decisions:** the umbrella was already recorded by parallel work — **D-031** (Mini becomes primary host; dashboard **+ Sortie together**; **lift-and-shift**; migrate-before-previews) — so I did NOT duplicate it. Recorded **D-034** = the migration *mechanics* D-031 had flagged "not yet designed":
- **Colima** (not OrbStack/Docker Desktop) — headless/launchd, OSS; VM `--cpu 6 --memory 12 --disk 100`; **Watchtower socket → `~/.colima/default/docker.sock`** under Colima.
- **Manual cutover:** stop both stacks → `VACUUM INTO` each DB (folds WAL, D-025 lesson) → `ssh cat` NAS→Mini (NAS has no SFTP) → checksum+verify → **NAS DBs frozen as rollback** until proven.
- **`.local` mDNS hostname + keep port 8088** (`scutil --set HostName`); one-time sweep of hardcoded `192.168.68.50:8088` refs.
- **Auto-login + LaunchAgent** boot (Colima → dashboard → sortie, ordered; `restart: unless-stopped` backstop). Colima is per-user → auto-login needed for unattended reboot; FileVault-off tradeoff accepted.
- **DJ library = NFS mounted directly into the Colima VM `:ro`, fast-follow** (avoids macOS-host→VM double-hop + SMB quirks). D-031's consequences line now points to [[D-034]]. Sortie egress containment ports as-is (same Linux Docker in the Lima VM) — a verify item, not a redesign.

**Board reconciliation** (prod, `192.168.68.50:8088`): **PD-188** ("Migrate PD from NAS to Mac Mini") was a **stub** ("…be sure to grill me") → **enriched** into the real ticket (links D-031/D-034 + ordered checklist). **Superseded PD-15/16/44** (preview build/run/GUI trio — obsolete under D-031's in-process-reconciler redesign): `[superceded]` prefix + status `completed` + note (mirrors PD-14's convention). **Created:** **PD-198** (`[Mac Mini Migration]` DJ library NFS fast-follow, P2), **PD-199** (`[Infra]` go-private + GHCR `read:packages` token on the Mini pull path, P1 — flipping private breaks the public Watchtower pull), **PD-200** (`[Mac Mini Migration]` Colima re-eval ~1mo + set a 2026-08-01 reminder once the reminders feature lands, P2). Left alone: **PD-190** (backup off-box target) + **PD-34 / D-030** (off-LAN via Tailscale) already existed.

**Preview feature:** deferred, not dropped — its redesign (in-process poll-reconciler on the Mini, builds locally/arm64 no GHCR push, deterministic per-PR URL, ~10 concurrent w/ evict-oldest, GH-Action-linked) lives in D-031; re-ticket after the migration lands.

**Reconciliation lesson:** heavy parallel work (worktrees `mission-control`, `pd-33-backups`) landed D-029→D-033 + PD-33/34/188/190 *while* this grill ran. Two mid-session writes of mine (a draft D-029 + 07-01 day-file entries) were rolled back by Steve. **Always re-read `DECISIONS.md` + the live board before recording decisions or creating tickets — the ground moves under long sessions.** D-NNN numbers in particular: my "D-029" draft got renumbered to D-031 by parallel work.
