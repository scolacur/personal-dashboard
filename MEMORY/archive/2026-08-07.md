# 2026-08-07

`/harness PD-382`. Board-hygiene session — no code, all board edits via the Task Monitor API.

## PD-382 closed out — its one loose ticket was the deliberate leftover

PD-382 (Dev Ops restructure) had 8 members: 6 completed, PD-412 closed, and **PD-416** open. That
one was never orphaned work — PD-412's own body says the web route rename ships and *"the endpoint
rename is a separate deferred P4 ticket."* Moved it to **PD-454 — [Epic] Misc refactorings**, whose
charter literally names "renames" and says its tickets are independent and pickable in any order.
It now sits with PD-258 (rename packages→lib) and PD-445 (rename the repo).

Rejected PD-365 (board *features*, not cleanups) and PD-449 (deploy/CI/container, not naming).

## PD-365 split into 3 new epics + 4 existing ones, and it closed itself

57 members, 21 open — too big to navigate. **The mechanic that made this cheap: a non-empty Epic's
lane is derived on read** (`deriveEpicLane`, D-054) — all-terminal members with at least one
`completed` derives **Completed**. So moving only the 21 open members out left 36 terminal ones and
PD-365 dropped into the Completed band on its own. The 36 stay put as the historical record; there
was no need to re-home shipped work.

New: **PD-493** board views & interactions (8), **PD-494** Epics v2 (4), **PD-495** Dev Ops
Configuration page (3). Reused: PD-451 ← PD-45/PD-47, PD-450 ← PD-244, PD-418 ← PD-248,
PD-492 ← PD-319. Full mapping table is on **PD-365's body** as a closeout banner — don't rebuild it
here.

Two placement calls worth keeping:

- **PD-158 is not a Reminders Widget ticket.** The title reads like PD-357's epic, but its body is
  `agent_ticket_reminders` — reminders *on a ticket*, a board feature. Went to PD-493.
- **PD-45 is the framework, not a job.** PD-451's charter says every job it holds needs "a
  schedule, a run store, and a readout on the Dev Ops Jobs surface" — PD-45 builds exactly that, so
  it belongs under the epic that consumes it rather than in a generic infra bucket.

**C-33 — Steve deleted it, same session.** "Seeds-style durable work queue [Warren Rec 9]",
`source: seed:core/META-TODOS.md` with `projectId: 1`. Flagged rather than re-homed because its
body defers to Sortie, which is retired, and D-055's DB-is-the-queue arguably already *is* the
durable queue; Steve's answer was to delete it outright.

**The pattern is what matters, and it is not exhausted.** Core tickets carrying `projectId: 1`:
C-3 (found 2026-08-06, on PD-418), C-33 (this session, deleted), plus **C-87** (on PD-451) and
**C-29** (epic-less) still sitting there. Sweep `displayId LIKE 'C-%' AND project_id = 1` once
rather than catching them one at a time — each has been surfaced by accident so far.

## Answered: the add/remove-widgets ticket is PD-334

**PD-334 — "[Shell] Arrange V2 — add/remove widgets per page (widget library)"**, backlog/P3 under
PD-452. V2 of the completed PD-331 (which is reorder + resize only). The real work in it is turning
page membership from the hardcoded `widgets.ts` `pages: string[]` into persisted user-editable
state, not the palette UI.
