# 2026-07-30

One continuous session spanning 07-28 → 07-30; entries below cover the whole run.

## Claude account deleted → credential rotation (07-28)

The Claude account behind the agent-worker was deleted with its email; new account is
**neonwind421@proton.me**. Findings that matter next time:

- The live credential is **`CLAUDE_CODE_OAUTH_TOKEN`** (`sk-ant-oat01…`, a Pro/Max subscription
  token from `claude setup-token`), NOT `ANTHROPIC_API_KEY` — which is unset. So "session limit"
  errors are the *subscription* quota, shared with Steve's interactive Claude use.
- **GitHub was unaffected.** `GITHUB_READ_TOKEN` → `scolacur`; `ROBOT_GITHUB_TOKEN` → the
  `sortie-bot-55` bot PAT (`public_repo`). Both still valid; do not revoke `ghp_ROg7…`, the Robot
  pushes with it. My initial "ROBOT_GITHUB_TOKEN is dead (401)" was a **test artifact** — I read the
  raw env line including its inline `# comment`; Compose's env_file strips those correctly.
- **Gotcha:** passing the token as a shell argument truncated it (the remote shell split it and ran
  the tail as a command). `/volume1/docker/personal-dashboard/set-claude-token.sh` on the NAS now
  takes it as `$1` *or* on stdin, refuses anything <100 chars as truncated, and timestamps a backup.
- Deleted the dead `sortie.env` (its GitHub token duplicated the live one) and Steve removed a
  **zombie DSM Task Scheduler job** that had been firing the C7-deleted `ops/sortie/quota-refund.sh`
  every ~15 min since 07-17.

## Skill rename `/core-session-start` → `/harness` (CORE D-028)

Renamed in the CORE repo (`git mv`, global symlink re-pointed, live refs swept). Rationale + the
full touched-file list are in **core/DECISIONS.md D-028**. Board ticket PD-306 retitled. History and
`MEMORY/` deliberately keep the old name, same convention D-011 set.

## The path-guard doesn't exist — and the docs claimed it did

Reviewing PR #268 (PD-402, Spotify client) surfaced that an autonomous Robot added a dependency to
`apps/server/package.json`, *correctly identified it as sensitive*, wrote in its PR body that "the
path-guard will flag this", and merged with **no such check existing**. There is no
`.github/sensitive-paths.txt`, no guard workflow, no `sensitive-change-approved` label — PD-308
(Tier 1) and PD-312 (Tier 2) are both unshipped.

Root cause of the agent's belief: **PROJECT.md §9 and DECISIONS D-047 described the model in the
present tense.** Both now carry explicit "NOT YET IMPLEMENTED" banners telling agents they are *not*
operating under that safety net. PD-308 promoted to `prioritized`/P1 with the incident recorded.
**Lesson: docs an agent reads as ground truth are themselves a safety surface.**

## Quota exhaustion — three times in two days → agents disabled

PD-402, PD-420 and PD-426 all hit `You've hit your session limit`. Steve's new plan has a lower
token budget, so **robot dispatch is now paused** (`/robot/pause`, reason recorded on the loop) until
the hardening work lands. Note **everything shares one account**: this Claude Code session, the
refine sessions, and robot runs all draw on the same quota.

Two structural findings worth acting on:
- **A deterministic park outlives its cause.** PD-420 parked on a session limit at 21:45 ET; quota
  reset at 1:30 AM ET; it sat idle ~12h until manually unstuck. The error text carries the reset
  time verbatim (`resets 5:30am (UTC)`) — parseable. Folded into **PD-411** (promoted P1) as a cheap
  reset-aware-retry slice that doesn't need multi-plan support. Closed **PD-36** as its duplicate.
- **A queued ticket with `agent_state='stuck'` can never dispatch** — `select.ts:51` gates on
  `agent_state IS NULL OR 'queued'`. Queueing from the UI does not clear it; it needs an Unstick.
  Silently-undispatchable is reachable from the board.

## Dev Ops route restructure — PD-412 decomposed, all three slices shipped by hand

PD-412 blew the 50-turn ceiling twice (run #15: 43 turns; #16: 50, both `no-verify` — the agent
believed it was done and never wrote `.robot/verify-ok`). Decomposed into PD-420/421/422 sized so
each leaves `verify` green, then **done by hand** after agents were disabled:

- **PD-420** (#270) — `/task-monitor` → `/devops`, 42 refs across 23 files. Done as a **scripted
  API-guarded sweep**: a naive `s#/task-monitor#/devops#g` also rewrites the 10
  `/api/widgets/task-monitor/*` strings and breaks every API call. Green first run.
- **PD-421** (#271) — Kanban extracted to `/devops/task-tracker`; overview 1116 → 17 lines. Helper
  modules stay in `devops/` (api.ts has 7 consumers incl. three `lib/` components; RelationPicker is
  used by the ticket-detail page).
- **PD-422** (#272) — agent-dashboard shell, all nav children now real subroutes, `#site-status`
  removed. DeployStatus/SystemStatus left in the repo, unmounted, awaiting PD-414/PD-413.
- **#273** — nav-caret highlight, Jobs page max-width, Jobs mobile overflow.

**The sizing lesson:** PD-420 didn't need a bigger budget, it needed a different *method*. Two robot
runs failed doing it file-by-file; as a scripted sweep it was one pass.

## Tickets filed this session

PD-423 (test the real Spotify wiring path), **PD-424** (auto-merge bridge deadlocks on its own
check — it reads its own in-flight check as UNSTABLE and skips; only bites approve-after-CI-green),
PD-425 (deadlines + Smart Sort), PD-426 (make `no-verify` failures diagnosable — currently
impossible: no captured output, worktree wiped by the PD-340 pristine reset, transcript image-local
behind sudo), PD-432 (per-ticket `max_turns` + Refine estimates the budget), PD-433 (Refine gets
read-only board access via MCP tools, not Bash), PD-435 (Refine may update *existing* tickets).

## Gotchas

- **git push as the wrong account:** the macOS keychain handed back `scolacurcio` (Splice) for a
  `scolacur` repo. Fixed repo-locally by routing git through `gh` (`credential.helper` = `""` then
  `!gh auth git-credential`). The same trap is waiting in other personal repos on this machine.
- **Self-authored PRs can't be self-approved** — merging them needs `--admin`.
- NAS clock runs **UTC−5** while the Mac is EDT, so NAS timestamps read an hour behind.
