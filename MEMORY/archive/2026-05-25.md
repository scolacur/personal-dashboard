# Memory — 2026-05-25

## 16:00 — Reconciled misplaced agent-dashboard PROJECT.md

`pages/agent-dashboard/PROJECT.md` contained an older planning document for the entire Personal Dashboard (written around a different environment: Mac Mini M4 Pro, Tailscale, n8n, PostgreSQL, Drizzle). This was discovered and reconciled: the file was rewritten to be scoped specifically to the agent dashboard / Mission Control page.

The new PROJECT.md captures: 4 Phase 1 screens (Tasks, Activity Feed, Errors, Inbox), Phase 2 screens (Scheduled Jobs, Sub-Agent Monitor), future improvements (Projects, Memory, Docs, Team, Visual Office), the `agent_jobs` / `agent_errors` / `agent_inbox` / `agent_schedule` SQLite schema, and the key design principle that the dashboard is orchestrator-agnostic (reads from whatever writes to those tables).

## 16:15 — Added D-010, D-011, D-012 to DECISIONS.md

Three new entries capture deferred architectural decisions from the old planning notes that are relevant if the project moves to a fully autonomous multi-agent setup:

- **D-012** — DIY Symphony multi-agent coding workflow (task files, git worktrees, semaphore, fan-out) — deferred until manual triggering becomes the bottleneck
- **D-011** — n8n as workflow orchestrator — deferred; dashboard is intentionally agnostic; revisit when scheduling needs outgrow scripts or external webhooks are needed
- **D-010** — PostgreSQL — deferred in favor of SQLite; flagged as the upgrade path specifically for `agent_*` tables when concurrent agent writes become a bottleneck

User chose "SQLite for now, flag as a future upgrade" when asked directly about the database decision.

## Later — Architecture session: Symphony + Mission Control decisions

Three key decisions made (D-013, D-014 added to DECISIONS.md):

1. **Symphony is a standalone project** — lives in `multi-agent-linear-workflow/`. Node.js service. Not inside CORE (build artifacts don't belong there).
2. **Claude Code as the agent runner** — `claude --print` CLI subprocess replaces Codex app-server. Linear MCP already handles what the spec's `linear_graphql` extension would have built.
3. **Mission Control UI stays in Personal Dashboard** — it's a page that calls Symphony's `/api/v1/state` API. Data (agent_jobs, agent_errors, agent_inbox) lives in Symphony's SQLite, not the Dashboard's. Supersedes the old `Projects/mission-control/` standalone plan from CORE META-TODOS.

CORE's META-TODOS has a "Mission Control" section that needs updating to reflect D-014. Symphony spec in `multi-agent-linear-workflow/SYMPHONY_SPEC.md` is the implementation blueprint — no code written yet.

D-012 ("DIY Symphony deferred") is effectively superseded now that we're actively planning Symphony. The approach is different (Symphony spec + Claude Code, not tasks/ folder + n8n + worktrees) but the intent is the same.

## Later — Session start (matrix-session-start)

Session bootstrapped. PROJECT.md loaded (monorepo scaffold + Music Tracker matcher steps 1–4 done per last commit). Memory loaded from today and yesterday. Created SKILLS.md and `.claude/commands/` stubs (wrap-up, project-maintenance) — these were missing.

Active memory target for this session: `/Users/steve/Documents/Dev/projects/personal-dashboard/MEMORY/`
