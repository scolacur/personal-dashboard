# 2026-07-06

## Card status-row redesign — refine + sortie pills (PR #193, `feat/card-status-row-pills`)

Reworked the Task Monitor `TicketCard` status row (below the title) into two sides: **Refine state left (outlined pill), Sortie state right (filled/tinted badge)** — `justify-content: space-between`, always-present `.status-left` slot pins the sortie badge right even when there's no refine state. Moved the refine pill down out of `card-top-right`.

- **Removed the ✨ Sparkles action button.** Refine lifecycle now lives entirely in the left pill: **Not refined** (muted→accent hover; backlog/prioritized only; onclick starts refine — same `onRefine`) → **Grilling…** → **Needs you** → **✓ Refined**.
- **Filled vs outlined = which agent:** sortie badge `background: color-mix(in srgb, currentColor 16%, transparent)` (tint auto-tracks each state's `color`); refine pill stays a hollow outline. color-mix is the established idiom (Button.scss/pomodoro).
- **Colour semantics:** ✓ Refined → green `--status-done` (was mis-mapped to yellow `--status-working`); sortie `needs-human` shares `stuck`'s red (both blocked/urgent); Refine **Needs you** → NEW token **`--status-refine-needs: #d6336c` (pink)** in global.scss — wants input, less urgent than a red stuck job; sortie `awaiting-human` stays calm light-orange. Resolves the "both agents say awaiting-human" confusion (pink-outline vs orange-fill, different side).
- Verified via a **static CSS harness** (all states × light/dark, screenshotted with headless `/Applications/Google Chrome.app` since no chromium-cli/playwright installed) + live dev board. Dev dummy data has NO `agentState` and no not-refined backlog ticket, so the harness was needed to see filled sortie badges / Not-refined.
- Dev stack = `npm run dev` (concurrently: server :8080 + vite; vite fell to :5174 since :5173 was already taken by a pre-existing session). vite proxies `/api`→:8080.

## Task Monitor board — extracted testable board-logic (re PR #171)

Reviewing PR #171 (pure markup+CSS refactor: inline `<article class="card">` → `TicketCard.svelte`). Question was "why no tests updated?" — because the **web suite tests pure-TS logic helpers only**; [vite.config.ts](../apps/web/vite.config.ts) deliberately strips the SvelteKit plugin, so there is **no component-rendering infra** (`@testing-library/svelte`/jsdom absent). A refactor that moves DOM+styles but no `.ts` touches nothing the suite runs.

Found a real gap: the fiddliest board logic lived **inline & untested** in `task-monitor/+page.svelte`. Extracted `isStatusLocked` + `computeSortOrder` (drag-drop fractional midpoint reorder) into **`task-monitor/board-logic.ts`** (`computeSortOrder` made pure — takes `byStatus(status)` list as a param instead of closing over state; also moved `AGENT_CONTROLLED` const there). Added `board-logic.spec.ts` (11 tests). Web suite 56→67 tests, 7 files; svelte-check 0 errors, eslint clean.

**Sequencing:** Steve merged #171 first (the big TicketCard extraction); I re-applied board-logic on a clean branch **`refactor/board-logic-tests`** off updated `main` (the WIP had been stranded on `fix/griller-stale-resume`). #171 left the extracted logic untouched — it only changed a call site — so the re-apply was clean.

- Pre-existing (not mine): svelte-check warns `.card-actions button svg` unused CSS selector in +page.svelte — orphan from #171's card-markup move.
- Extraction pattern already established: `sort-logic.ts`, `filter-logic.ts`, `copy-utils.ts` — board-logic follows it. Remaining inline/untested: lane-visibility persist, `onColumnDragOver` band boundary.

PR #183 merged into main (squash `6059bde`); board-logic now in main. Also deleted stale `fix/griller-stale-resume` (its one griller stale-resume fix already shipped via #166 squash-merge; branch was 9 behind main).

## Resolved merge conflicts on PR #172 (shared Button component, `sortie/168`)

#172 adds `$lib/Button.svelte` (primary/ghost/icon) + migrates task-monitor buttons. Conflicted with #171 (TicketCard extraction) + #183 (board-logic). A sortie bot had already pushed a self-resolution commit (`2180173`) that manually pulled main's content (TicketCard.svelte w/ Button, filter-logic, etc.) into one commit — but that **diverged history so GitHub still flagged CONFLICTING**. Merged `origin/main` into the branch in a scratch worktree; only real conflict left was the import block (keep both: board-logic + Button). Verified: Button usage 5 in +page.svelte (toolbar/modal) + 5 in TicketCard (card icons); svelte-check 0/0, 72 web tests pass, eslint clean. Merge `52bbec2` pushed → PR now **MERGEABLE**.

- Worktree gotcha (again): fresh worktree has no node_modules → symlink main repo's `node_modules` (+ per-workspace ones) AND run `npx svelte-kit sync` before `svelte-check` (else it errors on missing `.svelte-kit/tsconfig.json`).

## PD-281 Ticket Audit — grilled to a design (D-045) + sliced

Grilled PD-281 (`/grill-with-docs`) into a full design, shipped as **D-045** (see `DECISIONS.md`). One-line: an autonomous, recurring (weekly Mon 05:00) **agent-worker** job, read-only run → human-gated apply, two-entity model (`audit_run` + living/sticky `audit_finding`), adversarial verify + confidence-gated one-click, verbs Accept/Reject(+reason)/Send-to-Refine, surfaced at `/reports/task-audit`. Rationale/alternatives are all in D-045 — not repeated here.

- **Amended D-044**: Refine now launches from **backlog or prioritized** (was prioritized-only) so "Send to Refine" can escalate a backlog finding; the **griller worker is generalized into the `agent-worker`** (hosts `refine` + `audit` jobs). Glossary (agent-worker, Job) added to PROJECT.md §8; Grill/Prioritized defs relaxed.
- **Sliced** into epic PD-281 → **PD-282…PD-289** (board backlog, **P2**). Chain: 282→283→{284,285,286}→287→{288,289}. PD-288 gated on the relations PR; PD-289 on PD-250 + the refine job. Relations between slices are text refs for now (no relation-write API yet — that's PD-288's own dep).
- Docs shipped as **PR #161** (`wip/d-045-ticket-audit` → main, docs-only). D-045 rides ahead of the first slice, like D-044 → #149.

## Dogfood run of the existing `/core-ticket-audit` skill (206 tickets)

Ran the existing read-only audit skill across PD + Core (177 + 29 active) to inform PD-281's design. Headline (not applied — read-only): **PD** 2 archive / 4 complete / 3 reprioritize / 8 update; **Core** 1 archive / 3 reprioritize / 4 update. High-conf actionables: PD-142→archive (superseded by PD-251), PD-240/PD-192/PD-205→complete, C-46→archive. Report mock (strawman for the UI): https://claude.ai/code/artifact/81e6af88-21e7-4166-a994-3a46b4c09e91. Findings weren't persisted — the audit job will regenerate them.

## Prod API base is `task-monitor`, not `agent-dashboard` (local main is behind)

Live prod (`http://192.168.68.50:8088`) serves the board at **`/api/widgets/task-monitor/...`**; the old `/api/widgets/agent-dashboard/...` base **404s on prod**. Local `main` source still uses `agent-dashboard` (dev :8080 = dummy data). Skills/docs referencing the old base silently fail against prod — the `/core-ticket-audit` skill's endpoint is stale. Also updated the Core `/core-session-start` skill to accept a `pd-NNN` ticket arg and look tickets up via this endpoint.

## Gotcha — concurrent session switched the branch under me

Mid-session another session checked out a different branch in the same worktree; my `git commit` of the D-045/PROJECT.md changes landed on `fix/griller-image-pino` instead of my `plan/pd-281-ticket-audit`. The other session relocated the commit to `wip/d-045-ticket-audit` (clean: just my 2 files). Lesson: re-check `git rev-parse --abbrev-ref HEAD` before committing when sessions may run concurrently in one worktree.
