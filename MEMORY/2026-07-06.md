# 2026-07-06

## PD-281 Ticket Audit — grilled to a design (D-045) + sliced

Grilled PD-281 (`/grill-with-docs`) into a full design, shipped as **D-045** (see `DECISIONS.md`). One-line: an autonomous, recurring (weekly Mon 05:00) **agent-worker** job, read-only run → human-gated apply, two-entity model (`audit_run` + living/sticky `audit_finding`), adversarial verify + confidence-gated one-click, verbs Accept/Reject(+reason)/Send-to-Refine, surfaced at `/reports/task-audit`. Rationale/alternatives are all in D-045 — not repeated here.

- **Amended D-044**: Refine now launches from **backlog or prioritized** (was prioritized-only) so "Send to Refine" can escalate a backlog finding; the **griller worker is generalized into the `agent-worker`** (hosts `refine` + `audit` jobs). Glossary (agent-worker, Job) added to PROJECT.md §8; Grill/Prioritized defs relaxed.
- **Sliced** into epic PD-281 → **PD-282…PD-289** (board backlog, **P2**). Chain: 282→283→{284,285,286}→287→{288,289}. PD-288 gated on the relations PR; PD-289 on PD-250 + the refine job. Relations between slices are text refs for now (no relation-write API yet — that's PD-288's own dep).
- Docs shipped as **PR #161** (`wip/d-045-ticket-audit` → main, docs-only). D-045 rides ahead of the first slice, like D-044 → #149.

## Dogfood run of the existing `/core-ticket-audit` skill (206 tickets)

Ran the existing read-only audit skill across PD + Core (177 + 29 active) to inform PD-281's design. Headline (not applied — read-only): **PD** 2 archive / 4 complete / 3 reprioritize / 8 update; **Core** 1 archive / 3 reprioritize / 4 update. High-conf actionables: PD-142→archive (superseded by PD-251), PD-240/PD-192/PD-205→complete, C-46→archive. Report mock (strawman for the UI): https://claude.ai/code/artifact/81e6af88-21e7-4166-a994-3a46b4c09e91. Findings weren't persisted — the audit job will regenerate them.

## Prod API base is `task-monitor`, not `agent-dashboard` (local main is behind)

Live prod (`http://192.168.68.50:8088`) serves the board at **`/api/widgets/task-monitor/...`**; the old `/api/widgets/agent-dashboard/...` base **404s on prod**. Local `main` source still uses `agent-dashboard` (dev :8080 = dummy data). Skills/docs referencing the old base silently fail against prod — the `/core-ticket-audit` skill's endpoint is stale. Also updated the Core `/core-session-start` skill to accept a `pd-NNN` ticket arg and look tickets up via this endpoint.

## Gotcha — concurrent session switched the branch under me

Mid-session another session checked out a different branch in the same worktree; my `git commit` of the D-045/PROJECT.md changes landed on `fix/griller-image-pino` instead of my `plan/pd-281-ticket-audit`. The other session relocated the commit to `wip/d-045-ticket-audit` (clean: just my 2 files). Lesson: re-check `git rev-parse --abbrev-ref HEAD` before committing when sessions may run concurrently in one worktree.
