# 2026-09-03

## 13:50 — Board went unreachable mid-create; nothing partial was written

Steve went off his wifi network. The board is at `192.168.68.50:8088` and is **LAN-only by design**
(PROJECT.md §4), so it was unreachable rather than down.

**Nothing partial was created.** The failure was a `TimeoutError` on **connect**, on the very first
POST of the batch, so no request ever reached the server — no orphaned Epic, no half-built member
tree, nothing to reconcile. Worth recording because a mid-batch failure *would* have needed a
reconciliation pass and this one did not; the read was confirmed on 2026-09-06 (board still at 779
tickets, highest id PD-692, exactly as left).

The owed Epic's full content was written into the session inbox rather than left in the scratchpad,
which is session-scoped — the reason it survived three days and a context boundary intact.

## 14:05 — The outage became its own ticket

Losing access to his own task board by leaving the house is the argument for making it reachable
off-network. Steve tied it to the **Mac Mini migration**. Recorded, with the instruction to **search
before filing** — PROJECT.md §1 already lists off-LAN access as a post-MVP goal, so a ticket was
likely to exist. It did (see 2026-09-06).

## 14:10 — The Daily Log Epic was reframed before it was ever created

The "create some formulas that operate on the 2025 Daily Log" framing was **an outdated note**. Steve
corrected it: **the log gets imported into the dashboard**, because that is the point of the
habit-tracker and logging widgets, and he wants to **visualise** it.

That killed the Epic's main open question ("spreadsheet formulas or dashboard code?") before it was
filed, and raised a better one — whether the imported log lands in per-widget tables or one shared
store. It also surfaced the consequence with the most reach: **the 2025 log is the historical backfill
for widgets that already have Epics** (Habit Log, Workout Log, Sleep Log, Mood Tracking), which would
otherwise launch empty. The import format is therefore a constraint on all of them.

The outage turned out to be useful: the Epic was filed three days later in a materially better shape
than the version that failed to POST.
