# 2026-09-02

## 01:40 — The overnight Refine incident resolved itself once PD-618 was deployed

Steve rebuilt the agent-worker on the NAS. `buildSha` moved to `c49f91f` (the PD-618 merge),
`sessionLimit: null`. **PD-465 ([Epic] Misc Minor Bugfixes), stuck for eight hours, answered and
committed within three minutes of the restart** — a Populate creating 13 members, PD-619–PD-631.
The turn that had been failing every 5 seconds succeeded on the first attempt; the quota had rolled
over, so no hold was ever needed. The fix was still the right fix — the 5-second retry loop is what
burned the quota in the first place.

Worth noting: PD-465 shows `refineStale: false` despite gaining 13 members. Correct, not a bug —
D-089's invalidation only fires on an Epic that *currently claims* to be refined, and PD-465 never
made the claim.

## 02:00 — `refined` becomes a dispatch gate: five decisions, three tickets

Steve raised four improvements to refinement; grilling them changed two.

**The grounding is the part worth keeping.** `refined` gated **nothing** — the queue gate was `ready`
(the four-section body check, D-058) plus the D-051 blocker gate. And `needsQueueBypass` is
**client-side only**: the server's `updateTicket` never checked `ready`, so a `curl` PATCH — and
`approveRefine`'s own "Approve & queue" — already put an unformatted robot ticket in the Queue.
**10 tickets of 634 were refined.**

**The migration window was open**: the Queue held one ticket (PD-611, assignee `steve`) and **zero**
robot-assigned tickets, so adding `refined = 1` to `robotQueueCandidates` stranded nothing. It stops
being free the moment anything is queued.

Decisions:

1. **Hard gate on the ticket AND its Epic, no bypass.** `ready` keeps its bypass — "run it
   unformatted anyway" is a real want; "dispatch work nobody has read" is not.
2. **One shared predicate, three layers.** `queueBlockers()` in `packages/shared` beside `isReady`,
   returning an ordered criteria list. Route guard refuses and returns it; the web renders the same
   function's output; the loop query adds `refined = 1` as a backstop for the one case the guard
   cannot catch — a ticket un-refined *after* queueing.
3. **Refine children: agent declares, server verifies.** `refined` defaults true per child, downgraded
   if a robot-assigned child's body fails `isReady`. Human-assigned children skip that test.
4. **No fourth Epic state** — "refined but its members aren't" is derived, never stored.
5. **A Populate marks the Epic refined, because it now rewrites the Epic body.** Steve's synthesis,
   which dissolved my objection that auto-marking asserts "the description frames the work" on a body
   nobody rewrote.

**The argument that settled #4, because it will come up again:** auto-marking an Epic refined once
its last member is refined **defeats D-089's invalidation** — add a member → un-refines; refine that
member → silently re-refines, and the human never sees the invalidation the mechanism exists to
raise. Steve's stated instinct was the auto-mark; the argument reversed it. #5 is not the same thing:
that one is the Refine session itself, where the claim originates.

**The ordering trap, before it bites a third time:** `createTicket` fires `invalidateEpicRefinement`
per joining member, so a Populate must mark the Epic refined **last**. This exact bug was hit while
building PD-610 — in a test fixture, which is the only reason it was caught.

Filed under PD-493 ([Epic] Task Tracker board — views & interactions): **PD-632** (gate the Queue on
refinement), **PD-633** (the criteria-checklist refusal modal; `PD-632 blocks PD-633`), **PD-634**
(a Refine session's output is born refined). Amended **PD-611** and **PD-603**, which both
contradicted the above — PD-611's reversed "Out of scope" line is struck through in place rather
than deleted, so the reversal reads as deliberate.

Also settled without a ticket: **✓ Mark refined on a Ticket is now more necessary, not less** —
approving a `refine_in_place` already auto-refines, so the button is the only route for a
hand-written ticket, which the hard gate makes the common case.

## 02:25 — PD-611 shipped (PR #389, merged as `eae3ac0`)

The stale-Epic surfaces: banner on the detail page in the fixed stacking order (below frozen, above
"Part of epic"), `⚠ Stale` on the Epic band card and member rows, the members-section explanation for
a paused Epic, glossary entries in both the in-app modal and PROJECT.md §9, the refusal on both
re-queue gestures, and the three-way pause modal.

**The server change worth remembering:** `invalidateEpicRefinement` **always** paused, so
"Add & keep running" was not expressible. It now takes `pause` (default true), reachable from the
POST/PATCH ticket routes as a **`keepEpicRunning` body key** — safe because both routes build their
input from an explicit allowlist, so an unknown key is already ignored and this is the one place it
is deliberately read. It only ever weakens the default, never the un-refine itself. Declining logs
`epic_pause_declined`, applying D-089 §8's measurement argument to this choice.

`staleQueueRefusal` was kept **separate** from `needsQueueBypass` rather than merged into it, so
PD-632's `queueBlockers()` replaces it by deletion rather than untangling.

**Drive-by:** the glossary rendered ✓ Refined in `--status-working` (amber) while every real badge is
`--status-done` (green) — a legend showing a colour the board never uses, and actively misleading
once amber started meaning "stale". PD-631 covers the rest of that audit.

Verify baselines measured by stashing, not recalled: web 439 → **462**, server 814 → **818**,
agent-worker 504 → 504 (untouched).

## 10:40 — Searching the board by Steve's phrasing instead of by concept produced a confident wrong answer

Asked to confirm two tickets existed, I searched 674 tickets for `consisten*` and reported that
"consistency across tickets in the queue" had never been filed. It had: **PD-612 ([Robot] Check the
Queue for conflicting and duplicate work before it runs)** — Backlog, P1, assignee `steve`,
`ready: true`, `refined: false`, under PD-609 ([Epic] Robot loop hardening v2.5). Its body never uses
the word "consistency"; it says *conflicting and duplicate work*.

**Search the domain synonyms before concluding something was never filed.** On a board this size a
single-vocabulary substring search is not a search for the concept, and "it doesn't exist" is the one
answer nobody double-checks.

## 11:00 — PD-642 filed: "remove all sortie references" is the wrong instruction as written

Steve scoped the sweep to live code, wire fields, and non-terminal ticket bodies (seed data folded in
after a follow-up). Filed **PD-642 ([Cleanup] Retire the remaining live `sortie` references — code,
wire fields and open ticket bodies)** under PD-454 ([Epic] Misc refactorings), with
**PD-531 blocks PD-642** so the wire-field rename lands first.

**Dispatching the literal instruction would break production.** The 58 remaining files split four
ways and only the last is removable:

1. **`sortie-bot-55`** — the Robot's real GitHub login + commit email (`agent-worker/src/shared/config.ts:211-212`,
   `BOT_LOGIN` in `robot-auto-merge.yml:112`). The auto-merge trust check compares the literal.
2. **`sortie:queued` / `sortie:in-progress`** in `ACTIVE_SORTIE_LABELS` (`github-sync.ts:48`) — real
   labels on real issues; changing the values stops close-sync stripping them.
3. **`sortie_enabled`** in `schema.ts:182-188` — a *migration*; it must keep naming the old column.
4. Historical comments (~11 files under `jobs/robot/`) that correctly name the retired runtime.

So the ticket is a **classification** task, not a replace, and every load-bearing string that stays
gets a comment saying why. PD-263 and PD-278 (the `[Karpathy Memory]` Sortie tickets) are triage, not
rename — they spec work *on* the retired runtime.

## 11:35 — PD-617's missing blocker was a circular deferral, not the ticket anyone guessed

Steve believed PD-617 (Detail page V3: remove desktop Details/Refine tabs) was blocked by PD-629
([Ticket modal] "Save & Refine" button). It was not. PD-629 is a different modal — different Epic
(PD-465), different entry point (the create/edit form, not the header pill), and its Out of scope
says to leave the detail-page refine pane as-is, which is the exact surface PD-617 deletes.

**The real defect: nobody owned the desktop Refine modal.** PD-603 says *"The modal itself is
PD-605's… whichever of the two lands first builds it"*; PD-617 says it moves controls *"into the
modal surface provided by the 605 work"*. Each deferred to the other and PD-605 had no such member.

Filed **PD-643 (Detail page V3: the shared desktop Refine modal — the surface PD-603 and PD-617 both
defer to)** under PD-605, scoped to *add* the surface only and to own the shared component so PD-629
cannot fork a second one. Edges: `PD-603 blocks PD-617`, `PD-643 blocks PD-617`,
`PD-643 relates PD-629`. Amended PD-617's body to name both real blockers.

## 12:10 — Board-wide audit of prose blockers, and the method bug that made me mis-report two

Ran the sweep early rather than waiting on PD-612. Of 392 non-terminal tickets, 34 used blocking
language and 15 already had a correct edge. **Three were genuinely missing and are now drawn:**
PD-595 → PD-597 (Pomodoro colors before the circle timer), and the chain
PD-613 → PD-614 → PD-615. With PD-603 and PD-643 → PD-617, PD-605 now has a complete ordering.

**I reported two that already existed** — PD-455 → PD-460 (since 2026-08-04) and C-45 → C-102 (since
2026-08-27). My audit tested whether *the ticket containing the blocking sentence* had an inbound
edge, but in both cases the sentence lived in a different ticket than the blocked party. **The
subject of the sentence is not the subject of the edge.** Caught only because `addRelation` returned
pre-existing row ids — `UNIQUE(from, to, type)` made the re-request an idempotent no-op, so nothing
was duplicated and nothing warned me. **PD-612's job must key findings off the blocked ticket, not
off whichever ticket narrates the dependency.**

Three claims were moot (blocker already terminal: PD-283; PD-397/398/399). One is unresolvable:
**C-90 cites "`Harness portability & version control` (C-7)" but C-7 is *Replicate Core's dev tooling
into the active projects*.** Amended C-90 with a ⚠️ flag rather than guessing — drawing an edge on an
unverified reference is the failure the id+title convention exists to prevent.

## 12:20 — PD-645, and D-080 silently overriding a supplied priority

Filed **PD-645 ([bugfix] [Task Tracker] Epic band drag shows no insertion line — the card below
shifts up instead)** under PD-493. **Sent `priority: P2`; the board stored P1** — D-080 makes a
member's priority its Epic's (`store.ts:382-387`) and PD-493 is P1. Not a bug, but silent: the POST
returns 201 with the overridden value and no warning.

The bug has a lead. PD-602 unified five drop indicators into one `.drop-line`
(`apps/web/src/routes/devops/_drop-line.scss`) and the Epic band already has correct markup
(`task-tracker/+page.svelte:1081`). "No line, card below shifts up" is the signature of that element
being **in flow with its negative `margin-block` applied but its height/background not painting**.
Ruled out the obvious cause (`.epic-cell` gap is already `--space-sm`, the partial's default) and
pointed the ticket at the two real differences from the working Ticket band: Svelte scope class on a
`.drop-line` rendered into `EpicLane`'s children snippet, and `.epic-cell`'s `overflow-y: auto`.

## 12:50 — PR links: two tickets asked for, three filed, because the data does not exist

Steve wants a ticket's PR linked from its card and detail page, for robot- **and** human-completed
tickets, in any lane. **There is no PR association for human work at all:** `pr_url` lives only on
`agent_runs`, created and written by the agent-worker (`jobs/robot/runs.ts:79`) during hand-off;
`agent_tickets` has `github_issue_*` and no PR column. Robot tickets resolve today via
`GET /tickets/:id/runs` and `pr_url` persists past merge, so "any lane" works for them for free.

Second constraint: `TicketCard.svelte` renders from the **list** payload and has no runs data, so
per-card resolution is an N+1. The PR must ride along as a sparse map, the pattern already used for
Epic summaries and `/relations`.

Filed under PD-493, all P1 by inheritance: **PD-646 ([PR links] Resolve a ticket's PR for both robot
and human work, and expose it on the board payload)**, blocking **PD-647 ([PR links] Ticket cards
link to their PR)** and **PD-648 ([PR links] Ticket detail page links to its PR)**. PD-646 owes a
`D-NNN` and carries the three candidates unranked at Steve's direction, plus the question that
separates them: *is a confidently wrong link worse than no link?*

**Process note:** the three bodies cross-reference each other, so I wrote them citing PD-646/647/648
before the tickets existed — the [[file-the-ticket-before-citing-it]] failure. The guesses were right
only by luck; PD-644 had already been taken by a concurrent session earlier the same session. I
verified all six cross-references resolved to the intended titles after creation. **File first, PATCH
the references in.**

## 13:10 — The blocker sweep re-run: five genuine edges the 12:10 pass never reported

Re-ran the morning's prose-blocker sweep before learning Steve meant the recurring Ticket Audit.
Worth having done.

**The 12:10 pass under-reported, and its own arithmetic says so.** Its entry accounts for 15
already-edged + 3 drawn + 2 mis-reported + 3 moot + 1 unresolvable = **24 of the 34 candidates it
claims to have found**; ten were dropped with no stated outcome, and the findings below sat in that
gap. Detectors also disagree — it counted 34 tickets using blocking language, a broader regex finds
**85** (29 naming another ticket in the same sentence). It was the *narrower* detector, not a
superset. **PD-612 ([Robot] Check the Queue for conflicting and duplicate work before it runs) must
emit one outcome row per candidate**, not a prose summary; that is the property that would have made
this visible. All 13 edges the pass claims it drew or found **do exist** — verified individually.

Drawn (relation ids 178–184): `C-6 blocks PD-350` (PD-350's body *asserts* the relation already
exists), `PD-188 blocks C-99`, `PD-540 blocks PD-130`, and `PD-643 relates PD-603` — deliberately
`relates`, since PD-603 reserves the right to open the existing pane rather than wait on the shared
modal, so `blocks` would over-constrain a P1.

**The one that mattered: `PD-632 blocks PD-603` and `PD-633 blocks PD-603`.** PD-603's own 2026-09-02
amendment says to call `queueBlockers()` "built by PD-632" and open "the criteria checklist from
PD-633" — so a P1 assigned to Steve was sitting ungated against a predicate that does not exist yet.

**C-90's unresolved C-7 reference resolved: it meant C-92 ([Epic] Harness portability & version
control)** — the exact phrase C-90 quoted. Title right, id wrong. The trap worth remembering: **C-7
(Replicate Core's dev tooling into the active projects) is a member of C-92** and its subject matter
also fits "porting the tool into other projects", so the wrong id was plausible rather than absurd.

**Method guard:** `addRelation` returns the *pre-existing* row id on a duplicate, which is exactly
what let the 12:10 pass report two ancient edges as newly drawn. Snapshot every relation id **before**
posting and diff after, so "NEW" is measured rather than assumed.

## 13:40 — Ticket Audit run 9 reviewed: 5 of 7 sound, 1 wrong, 1 mis-framed

Run 9 (10:59 EDT, 203s, `claude-opus-4-8`, 358 tickets): 7 findings — 1 archive, 1 reprioritize,
5 update.

**The report surface is read-only** — Accept/Reject lands in PD-287 ([Audit] Apply mechanics), still
backlog — so every finding sits `decision: undecided` with no `confidence` or `proposedChange`.
**A rejected finding cannot be recorded and therefore recurs next run.** The only way to retire one is
to change the underlying ticket.

**Both misses share one cause: the audit reasons from a ticket's title and opening lines, not its
whole body.** PD-247 was recommended for **archive** because its body opens "This spike's premise is
dead" — three paragraphs later it says *"Kept rather than archived,"* having already been repointed at
the live `robot/pr-state.ts` poll. PD-553 was called overdue on its title and today's date alone,
when the 2026-08-23 session had already confirmed coverage, located the key, proven the restore and
written the procedure — a day *early*. Fix that reading behaviour before trusting any ARCHIVE
recommendation; archive is the one whose cost is asymmetric. Note the audit *did* read MEMORY/ for
two other findings, so the gap is inconsistent evidence-gathering, not absent capability.

Applied: **PD-553** retitled (stale `Do on 8/24:` dropped) and narrowed to its four real residuals —
the true RPO (~2 days), Hyper Backup's own integrity check (last run **2026-01-25**), an app-boot
test, and deleting the decoy directories. **PD-620** rescoped to a *third variant* of the badge #389
shipped, not a second badge. **PD-465**'s intake list demoted to provenance under an authoritative
members table. **PD-247** given a keep-alive banner + fresh revisit date (2026-12-01). **PD-4 /
C-87 / C-29** linked `relates` (ids 185–187).

**PD-316 was worse than the finding said.** The audit asked for a sortie→Robot rename; the real defect
is that its enumerated controls are already owned by siblings in its own epic — PD-511 (runtime
toggles), PD-501 (allowlists widget), PD-315 (switch which Coding Agent the Robot uses) — and its
proposed route `/task-monitor/configure` contradicts its epic, PD-495 ([Epic] Dev Ops Configuration
page). Rescoped to the page shell those three land into. **Polling and job intervals turn out to be
owned by nobody** — noted in its Out of scope.

**Extra catch:** C-87 and C-29 are the only two `C-` tickets sitting in **projectId 1** (the other 83
are projectId 2), which is why a Core-prefixed ticket turned up inside a PD epic.

## 13:55 — PD-419 re-deadlined to 2026-09-27

Written as an explicit, dated extension rather than a silent date swap, because the reason the audit
caught it at all is that the original 2026-08-27 lapsed invisibly while it sat P1/backlog. The note
says to **escalate rather than extend** on a second lapse — two silent lapses means it is not
actually P1. Its Ollama option is gated on the Mac Mini (PD-188), which now also blocks C-99.

## Open

- **The Robot dispatch killswitch is still on** — `dispatch.paused: true`, "paused by human (nav
  killswitch)", since 2026-08-30. Untouched all session. Resuming is **two** flags.
- **PD-632 → PD-633** is the next sequence, and PD-632 owes a decision record (allocate the `D-NNN`
  when writing it — never pick one). It amends D-058's "`ready` is the dispatch gate" and extends
  D-089.
- **PD-612 is P1 and unrefined**, so once PD-632's gate lands it cannot be queued until marked
  refined — the highest-priority thing touched today.
- **PD-643 vs PD-629 overlap is held by `relates` edges and prose, not a gate.** If PD-629 is worked
  first nothing structurally stops it building a second modal. PD-643 now also `relates` PD-603.
- **PD-247 will be re-flagged for archive by run 10** regardless of the keep-alive banner — the banner
  is mitigation, the fix is PD-287's Reject+reason control.
- **A concurrent session ran throughout the afternoon.** The board went **681 → 736 tickets**: a new
  **PG** project (52 tickets), plus C-107, C-108 and PD-650. PD-465 gained PD-650 as a member between
  a snapshot and a write, so a members table built from the earlier fetch would have been wrong by
  one — **build derived content from a fetch taken at write time.** That session's inbox file
  (`runs/2026-09-02-session-1107.md`) was left untouched; it folds its own.
- PD-465's other 12 new members (PD-619, PD-621–PD-631) are unrefined in Backlog, which under the
  incoming gate means none of them can be queued until refined. That is the intended cost, but it is
  also the first real test of whether the gate is too expensive in practice.
