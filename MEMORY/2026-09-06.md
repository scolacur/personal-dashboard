# 2026-09-06

## 14:30 — PD-693 filed: the 2025 Daily Log import, and a convention that was never in conflict

Board reachable again; verified nothing had been created by the 2026-09-03 failure before filing
(779 tickets, highest PD-692, exactly as left).

**PD-693 ([Epic] Import the 2025 Daily Log and correlate it)**, P4, four members: PD-694 (import into
a shared store) → PD-695 (derived columns) → PD-696 (the correlations) → PD-697 (visualise).

**The storage question resolved, and the interesting part is that it was never a conflict.** Steve
chose a **shared daily-log table several widgets read**, framing it as relaxing PROJECT.md §5's
no-cross-widget-imports rule and guessing the rule's purpose was "avoiding spaghetti code". **It is
not a relaxation.** §5 forbids *widget-to-widget imports* — widget A reaching into widget B's folder —
and in the same sentence routes shared things to `packages/shared` (types) or `apps/server/src/lib/`
(server-side infrastructure), which is exactly where PD-442 put the shared `job_runs` store. A shared
daily-log store in `lib/` is the **documented pattern**, not an exception to it.

Two things genuinely do change, both on the Epic: this is the **first shared *domain* table**
(`job_runs` was infrastructure), so it **owes a `D-NNN`**; and §9's Firewall entry applies — a shared
access layer is **middleware, not a firewall**, since any widget can open its own `better-sqlite3`
handle. It must not get described as a boundary later.

The three statistical caveats survived from the original draft and are on the Epic: ~two dozen
predictors against Mood means **one in twenty looks significant by chance**, so decide in advance how
a finding earns belief; **correlation gives no direction** (the previous-day and rolling-window
framings are already a partial fix); and **variable types decide the coefficient** — Spearman is the
safer default for ordinal data. PD-695 carries the rule that matters mechanically: **lag and window by
date, never by row position**, or every gap in the log silently corrupts the rows after it.

## 14:45 — PD-34 is `completed` but off-LAN access demonstrably did not work

Searching before filing found **PD-34 ([Infra] HIGH — Reverse proxy for off-LAN access.)** already
**completed** — while Steve had just lost board access by leaving the house.

**Did not reopen it** (terminal is final) and **did not assert it was wrongly closed.** Two readings
fit and the evidence does not separate them: it may be genuinely done via Tailscale with Steve simply
not on the tailnet at that moment, or it was closed without the work landing. The one hard fact: it
carries **no `completedAt` and no `agentState`**, so it was hand-set completed rather than completed
by the Robot loop.

Filed **PD-698 ([Mac Mini Migration] Re-establish off-LAN access to the board — and raise the bar to
remote dev work)** under PD-188, P1 by inheritance, opening with *establish which reading is true
first*. **It is real work either way**, which is what made filing safe without resolving the
ambiguity: PD-34's setup is **host-specific** (its body weighs Synology's reverse proxy + DDNS against
Tailscale and cites the NAS port map), so the Mac Mini move invalidates it; the **bar rose** from
PROJECT.md §1's *phone access* to Steve's *remote dev work* — driving the control plane, not
read-and-tick; and **PD-34's own stated precondition was never met**, since its body says auth must
come first and PD-43 ([Dashboard Shell] Auth) is still backlog.

**The security argument worth keeping**, applying §9's Firewall test to infrastructure rather than
code: LAN-only is a *real* boundary because it is enforced outside the app. A tailnet preserves a
network boundary; a public reverse proxy with app-level auth **moves the boundary into the app**,
which is a far larger change than it looks. Remote *dev work* also widens what is exposed — the Dev
Ops surfaces and the Robot's controls, not just ticket cards.

## Open

- **PD-34's true state is unresolved.** PD-698 opens by asking it; nothing on PD-34 was changed.
- **PD-693 owes a `D-NNN`** for the first shared domain table — allocate it when the decision is
  written, never pick one.
- **Nothing created across this session is refined**, so none of it is queueable. Intended, not a gap.
- **The Robot dispatch killswitch is still on** (`dispatch.paused: true` since 2026-08-30). Untouched
  for the whole session. Resuming is **two** flags.
