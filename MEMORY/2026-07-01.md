# 2026-07-01

## 20:40 — Mac Mini M4 becomes primary host; branch-preview design grilled + deferred (→ D-029)

`/to-sortie-issues PD-15/16/44` (PR branch-preview feature: build half / run half / GUI half) turned into a `/grill-with-docs` design session that changed the whole approach. Key decisions:

- **NEW always-on Mac Mini M4 (24GB), arriving ~2026-07-03, becomes the PRIMARY host.** The Synology NAS demotes to a **storage/backup appliance** (keeps the DJ library + Hyper Backup→Backblaze; stops running the app). Rationale: NAS is CPU/RAM-weak (why deploy is pull-based to avoid building on it); the M4 is vastly more capable → enables ~10 concurrent previews, a better Sortie host, headroom.
- **Sequence: full migration FIRST, branch previews LATER.** Steve's call — previews are QOL (test Sortie PRs without switching off his working branch), not worth betting the Mini's first workload on. **Migration tickets get the prefix `[Mac Mini Migration]`.**
- **Migration ripples (surfaced, NOT yet designed):** dashboard + prod `dashboard.db` move to the Mini; DJ library becomes a **network mount (SMB/NFS)** from the NAS for music-tracker matching (previews don't need it — empty lib is fine except real-library matching); backups must follow the DB; **Docker on headless macOS via Colima/OrbStack, NOT Docker Desktop** (needs a GUI session); **GHCR pull auth breaks if repo goes private** (NAS/Mini pull would need a `read:packages` token — currently public/no-auth per [[2026-06-30]]).
- **Private-repo cost (Steve considering going private):** Actions minutes flip from unlimited→2,000/mo Free (3,000 Pro) then billed; GHCR private packages flip from unlimited→500MB+1GB/mo Free (2GB+10GB Pro) then billed. This feature adds ~nothing (previews build locally, no GHCR push; reconciler is self-hosted, not Actions minutes) — only the existing prod-image path is affected.

**Preview design decided (deferred, unbuilt — captured so it's not re-litigated):** orchestration = an **in-process poll-based reconciler** (state-reconciling, Watchtower-philosophy) inside the dashboard **on the Mini** — reads open PRs from GitHub + running `preview-pr-*` containers from Docker each tick, converges (start missing / stop closed / evict oldest when >~10). **Rejected:** self-hosted Actions runner (public-repo fork-code risk + event-drift), cloud preview (loses DJ library mount + breaks egress posture), dashboard-GUI-driven Docker (Fastify→Docker socket = security surface Steve hardened against). Previews **build locally on the Mini** (native arm64, no GHCR push — sidesteps NAS-amd64-vs-Mini-arm64 multi-arch); **deterministic per-PR URL** (port or Caddy hostname) so a GH Action can link it to the ticket with no polling; GUI = **read-only** "Previews" panel in Mission Control fed by the reconciler's SQLite state. NOTE: dashboard's `cron.ts`/`logger.ts`/`logs` table from PROJECT.md §2 are **NOT built yet** — the in-process cron + log-panel are net-new work.

**Original PD-15/16/44 NOT created** — superseded by the migration-first plan; will be re-sliced (in-process reconciler, no build/run split) after migration. `/to-sortie-issues` was mid-flight when the design pivoted.

## 20:26 — Session initialized (3rd session today)

New /core-session-start. PROJECT.md loaded (Node20/TS/Fastify + SvelteKit/SCSS + SQLite monorepo). Memory loaded — today's work held (shared-from-source refactor D-022→D-024, prod DB restore + seed-if-empty D-025, Mission Control board UX D-026/D-028, priority→P0–P5 + seed-from-prod). Aging pass: nothing to archive (all day files within 14d). Active memory target: personal-dashboard/MEMORY/.

## 19:43 — Session initialized (2nd session today)

New /core-session-start. PROJECT.md loaded (Node20/TS/Fastify + SvelteKit/SCSS + SQLite monorepo). Memory loaded — today's earlier work (shared-from-source refactor D-022→D-024, prod DB restore D-025, Mission Control board UX D-026/D-028) + yesterday's Sortie/CI-CD held. Aging pass: nothing to archive (all day files within 14d). Active memory target: personal-dashboard/MEMORY/.

## 09:43 — Session initialized

Project session bootstrapped via /core-session-start. PROJECT.md loaded (stack: Node20/TS/Fastify + SvelteKit/SCSS + SQLite; monorepo). Memory loaded — today's Agent Dashboard build + yesterday's CI/CD entries held. Active memory target: personal-dashboard/MEMORY/.

## 10:00 — Pomodoro logic moved out of `packages/shared`; `apps/web` got a test runner (D-022)

Steve pushed back on `pomodoro.ts` living in `packages/shared` — correctly. It was never shared: only the web component consumed it at runtime; the only other importer was a **test** in `apps/server`. It lived in `shared` purely so the server's vitest could reach it, because **`apps/web` had no test runner** — a testing-infra gap leaking into architecture. Fix (D-022): shared is now reserved for **wire types between server and web only**; widget-only logic lives with its widget.

**Changes:** moved `packages/shared/src/pomodoro.ts` → `apps/web/src/routes/widgets/pomodoro/timer-logic.ts` (next to `PomodoroTimer.svelte`); updated the component import to `./timer-logic`; dropped the pomodoro exports from `packages/shared/src/index.ts` and deleted the file; moved the spec → `apps/web/.../timer-logic.spec.ts`; deleted the server's `timer-logic.spec.ts`. Gave `apps/web` its own vitest (`vitest ^3.2.6` devDep + `"test": "vitest run"` + isolated `apps/web/vitest.config.ts`, no SvelteKit plugin). Root `test` now runs server **and** web. `npm run verify` green: **52 server + 16 web tests**.

**Vite-skew gotcha + resolution (D-022):** first pass used `vitest@3.2.6`, which peers Vite ≤7 while `apps/web` is on **Vite 8** → npm nested a second Vite (7.3.6) under `vitest`, and `svelte-check` errored on the two copies' conflicting global `ImportMeta`. Initially worked around it by excluding `src/**/*.spec.ts` in web tsconfig. **Then Steve asked to use the same vitest in both — did better: bumped BOTH workspaces to `vitest@^4.1.9`** (peers `^6||^7||^8`), which dedupes to the single Vite 8 already installed → no nested Vite, no conflict, **removed the tsconfig exclude entirely** (specs type-checked by svelte-check normally). Verify still green (52+16). Lesson: keep both workspaces on the same vitest major so no duplicate Vite reappears. Closes D-017's open follow-up; supersedes the pomodoro half of D-018.

## 10:17 — Fixed the prod MODULE_NOT_FOUND: `packages/shared` now emits Node-resolvable ESM (D-023)

Last deploy crashed on NAS with `MODULE_NOT_FOUND`. **Root cause (reproduced locally):** `packages/shared` was built with `moduleResolution: Bundler` → tsc emitted **extensionless** re-exports (`export … from './agent-dashboard'`). Bundlers (Vite/vitest/esbuild) resolve those, so dev + `npm run verify` were green, but **Node's native ESM loader requires file extensions** → `ERR_MODULE_NOT_FOUND` on `./agent-dashboard` the moment the server loaded `@dashboard/shared`. Exactly the dev/prod divergence D-019 flagged. Only surfaced now because the agent-dashboard widget made the server import shared **at runtime**: `routes.ts` imports the *values* `TICKET_STATUSES`/`TICKET_PRIORITIES` (not just types → real `require`). (`store.ts` is `import type` only, erased.) D-019's "server imports shared only in a spec, never at runtime" went stale.

**Fix (D-023):** shared → `module`/`moduleResolution` **NodeNext**, source uses explicit `.js` (`from './agent-dashboard.js'`), added an `exports` map to its package.json. Stays single-format ESM (browser needs ESM per D-019). CommonJS server loads it via Node's stable `require(ESM)` (Node ≥20.19; runtime is node:20-slim). **NOT a web-adapter issue** — `apps/web` already correctly uses adapter-static → `apps/web/build/` (gitignored, built in Docker, served by Fastify); nothing to change there (Steve's "adapter-node → build/" mental model was from a different project).

**Verified the REAL prod path**, not just a bundler build: `require('@dashboard/shared')` from CJS now returns the constants; **booted `node apps/server/dist/index.js`** against a temp DATA_DIR → `/api/health` = `{ok:true}`, no module errors. Full `verify` green (52+16). 📌 **Recommended follow-up:** add a server-boot smoke test (boot compiled server + curl `/api/health`) to the Dockerfile build stage or CI — `verify` builds but never boots the server, which is why this shipped (D-019's own open revisit note). Offered to Steve; not yet wired.

## 10:42 — Killed `packages/shared/dist` for good: consume shared from source, esbuild-bundle the server (D-024)

Modeled on Splice's `surfaces/apps/web-svelte` (Steve shared its config — it has **no dist for internal libs**; resolves them from source via tsconfig.base paths wired into Vite/svelte-kit). Did it in two levels, both verified.

**Level 1 — web consumes shared from source:** added `kit.alias` `@dashboard/shared` → `../../packages/shared/src/index.ts` in `apps/web/svelte.config.js` (one alias wires both Vite + generated tsconfig). Vite bundles source in dev AND prod → no dist for web, immune to D-019's browser crash. Proved by deleting `shared/dist` then building + `svelte-check`-ing web clean. This resolves D-019's objection to the alias (it rejected a *dev-only* alias for parity; unconditional alias = dev/prod both source-bundled, no divergence).

**Level 2 — esbuild-bundle the server, delete shared/dist entirely:** new `apps/server/build.mjs` (esbuild, **CJS** so `__dirname` works, `packages:'external'` to keep native `better-sqlite3` out of the bundle, `alias` inlines `@dashboard/shared` from source). `shared` is now **source-only**: `main`/`types`/`exports` → `./src/index.ts`, no build script; tsconfig back to `moduleResolution:Bundler` + `noEmit`; **reverted D-023's NodeNext/`.js` extensions** (only needed for Node loading dist, which no longer happens). Server tsconfig → typecheck-only (dropped outDir/rootDir); root `build` + Dockerfile drop the shared build step and `shared/dist` COPY.

**Verified:** full `verify` green (shared+web+server typecheck, lint, 52+16 tests); esbuild bundle **boots** (`/api/health` ok) with `shared/dist` deleted + better-sqlite3 native load OK; `tsx` dev resolves source. Bundle inlines shared (0 runtime `require('@dashboard/shared')`, TICKET_STATUSES present, 28KB). **Both prior dist bugs (D-019 stale-dist, D-023 extensionless-ESM) came from the bundler-vs-Node-native-loader gap; consuming source through bundlers everywhere closes it.** Tradeoff: shared is now only consumable by a bundler/transpiler, never plain `node` on a bare import — if a future Node entry imports shared without esbuild/tsx, bundle it or restore a build. Adapter-node/SSR (Splice's other choice) left OUT of scope — we're still adapter-static SPA served by Fastify.

## 11:36 — Prod DB restore + seed-if-empty guard + DEV banner (D-025)

**Prod came up empty after deploy** — root cause: the 246 tickets only ever lived in the **dev DB**; **data never syncs** dev→prod (the `data/` volume is gitignored, never in the image — only code + schema migrations propagate). Not data loss, not the D-024 refactor. Confirmed via prod API (over LAN, `http://192.168.68.50:8088`): 3 bootstrapped projects, 0 tickets, 0 music-tracker (so nothing to preserve). **Restored** by `VACUUM INTO` a clean single-file snapshot of the dev DB (folds the WAL; dev DB had 176KB main + 4MB uncheckpointed WAL — copying the .db alone would've shipped stale/empty!), streamed it over `ssh cat` (NAS has **no SFTP subsystem** → scp fails; also docker needs sudo-with-password there, dir is 777 so file ops don't), checksum-verified, then a `restore-db.sh` (stop app → backup → swap → start) which Steve ran with sudo. Prod now: **246 tickets, PD-1.., 49 completed.** Deleted restore-db.sh after (re-run footgun).

**seed-if-empty guard (D-025):** `seedIfEmpty(db)` in the agent-dashboard boot path — if `SEED_ON_BOOT=1` **and** `agent_tickets` empty, import committed `tickets.seed.json` (idempotent, shared `seedTickets()` with the CLI `import.ts`; JSON `import`ed so esbuild inlines it, bundle 28→139KB). Opt-in + empty-only = never fires in dev, never clobbers a populated board. **Must add `SEED_ON_BOOT=1` to the NAS `.env` to arm it** (documented in `.env.example`); dormant now (board has data). Answered Steve: no env line ⇒ no auto-seed even when empty (by design). Verified all 3 cases.

**Dev/prod confusion fix:** amber **DEV** badge + top stripe in `+layout.svelte` gated on `import.meta.env.DEV` (true only under `vite dev`, compile-time-off in prod build — no env wiring). Also **reset the dev DB to 5 obviously-`[DEV]` dummy tickets** (real 246 safe on prod + committed seed JSON + snapshot).

## Agent Dashboard "Mission Control" — cross-project ticket Kanban BUILT (Phase 1 + full schema + seed + archive)

Big build session. Branch **`feat/agent-dashboard-phase1`** (5 commits `cf5b62e`→`5f2a979`) — **NOT merged, NOT deployed to NAS**. `npm run verify` green throughout (52 tests, was 41). The TODO→Sortie-issue pipeline's Phase 1 (the board + data model) is done; **Phase 3 (convert-to-issue) is still pending**.

**What it is:** a **cross-project** Kanban at `/agent-dashboard` tracking tickets for ALL projects (personal-dashboard, core, nervous-system-website), reached via a "Mission Control" top-nav link. It's a PAGE, not a home-tile widget (not in `lib/widgets.ts`). 5 columns: Backlog/Ready (manual) · In progress/In review/Completed (**derived from GitHub `sortie:*` labels in Phase 3, not the Sortie :7678 API** — D-020). Project filter dropdown + color-coded chips. Reusable `lib/Modal.svelte` (backdrop/Esc/scroll-lock, a11y-clean) hosts the add/edit form.

**Data model (dashboard-owned; D-020, renumbered from D-018 after merging main's Pomodoro D-018/ESM-D-019):** `agent_projects` (slug, name, **key** PD/C/NSW, seq, github_repo, sortie_enabled, color) + `agent_tickets` (**display_id** `PD-7`/`C-3` per-project sequential, title, body, status, priority, project_id, **assignee**, **recur_interval**, source, sort_order REAL, github_issue_number/url, **archived_at** soft-delete, timestamps) + `agent_ticket_relations` (blocks/relates/duplicates — generalized), `agent_tags`+`agent_ticket_tags` (seeded UI/Infra), `agent_ticket_events` (activity log, populated from day one), `agent_ticket_reminders`. Backend widget `apps/server/src/widgets/agent-dashboard/{index,schema,store,routes}.ts`; shared types in `packages/shared/src/agent-dashboard.ts`.

**Migration framework — D-021 (`apps/server/src/migrate.ts`):** `_migrations` ledger + `migrate(db,id,fn)` (once, transactional) + `columnExists`/`addColumn`. **Schema only ever grows — never drop/recreate** (Steve's explicit "evolve the model without losing data" requirement). CREATE statements carry full current schema; addColumn migrations bring old DBs up (no-op on fresh). Unit-tested incl. a "migration preserves existing rows" test. This + the Backblaze backup TODO are the two data-safety pillars.

**todo→ticket rename** (Steve: "refer to TODOs as tickets"). Full rename across tables/types/routes/UI (`agent_tickets`, `AgentTicket`, `TICKET_STATUSES`, `/tickets`) — done pre-deploy so no data migration needed (`2d30cfc`).

**Seed (Phase 2) — 246 tickets, done + committed + ARCHIVED:** `seed/generate.ts` parses 24 PD `TODO.md` + core's `META-TODOS.md` → committed `seed/tickets.seed.json`; `seed/import.ts` idempotent replay (dedupe on **source+title**), sets display_id/status/source. **165 PD + 81 core; 49 completed** ("Shipped"/SHIPPED items → status completed). PD tickets get **`[<Domain>]` bracket prefix** from the file H1 (`[Pomodoro Timer Widget]`), root items `[Sortie]`/`[Infra]`, core unprefixed. +10 deferred-feature follow-up tickets seeded (relations/tags/reminders/recurring/assignee/drag-reorder/activity-feed/archived-view UIs + Phase-3 convert + GitHub-label sync). Live dev DB (`apps/server/data/dashboard.db`) seeded to 246; view at `/agent-dashboard` on the running dev server (8080).

**Archive (not delete):** all 24 PD `TODO.md` → **`/Users/steve/Documents/Dev/archive/TODO-<domain>.md`** (out of repo; git history + Backblaze-backed disk retain them). Repointed **CLAUDE.md** (session start no longer reads TODO.md — board is source of truth), **README.md**, **PROJECT.md**. **Core's `META-TODOS.md` intentionally NOT archived** — load-bearing (62 refs; core PROJECT.md §42 mandates it stay Markdown). Instead a **high-priority Core ticket** tracks migrating core to read its tickets from the dashboard API.

**Decisions made this session:** two HIGH infra tickets added first → now on board — **SQLite `.backup` consistent-snapshot cron** (Hyper Backup→Backblaze IS running & covers both DBs, but is not SQLite-aware; reuse `quota-refund.sh` `.backup` pattern) + **reverse proxy for off-LAN access** (Tailscale/Cloudflare Tunnel or Synology RP + auth). DB = shared `dashboard.db` SQLite, table-namespaced (D-009); upping Sortie concurrency does NOT force Postgres (agents write to Sortie's `.sortie.db`/GitHub, never `dashboard.db`).

**Still TODO (next):** merge branch (or dogfood via a Sortie issue), deploy to NAS (seed runs against prod DB via `import.ts`), then **Phase 3** (Claude-API convert-to-Sortie-issue: needs `ANTHROPIC_API_KEY` + write-scoped GH token on NAS) + the deferred UIs (now tickets on the board). Dev gotcha: web consumes `@dashboard/shared` from `dist/` → rebuild `shared` after editing. (My dev-only Vite alias was REMOVED during the main merge — main's D-019 made `shared` ESM and deliberately rejected the alias to keep dev/prod parity; deferred to that.)

## 11:56 — Mission Control board UX: Queued lane, drag-and-drop, search, full-width (D-026)

Steve iterated the `/agent-dashboard` board through several UX rounds. All shipped, all green (svelte-check 0/0, 52 server + 16 web tests).

**Page vs. section:** "Mission Control" is now the page `<h1>`; the board is a titled `<section>` "Tickets" holding its own controls (search / Condensed / project filter / + Add). Structured so future sections can be dropped onto the page.

**Queued lane (D-026):** added a `queued` status between `ready` and `in_progress`. Because `status` is a plain `TEXT` column (no CHECK) and API validation derives from `TICKET_STATUSES`, it was a 3-touch change: shared type+array (`packages/shared/src/agent-dashboard.ts`), server `ORDER BY` CASE (`store.ts`, reindexed 0–5), board `COLUMNS`. Board is now 6 lanes. **Rule: `TICKET_STATUSES` order = lane order = ORDER BY CASE — keep in sync.**

**Drag-and-drop (D-026):** native HTML5 DnD replaced the `◀ ▶` arrows (removed, along with `move`/`canMove*`). One `ondragover` per lane finds the insertion index by cursor-Y vs each card's midpoint; `onDrop` sets `sortOrder` = average of the two neighbours (±1 past ends). `sort_order` is a `REAL` → infinite fractional slotting, no renumbering. Visual: dragged card fades, target lane border highlights, accent insertion line above the drop-target card (or `.drop-end` for append). Trade-off logged: DnD is pointer-only now (no keyboard move path).

**Search:** live title+body case-insensitive filter (`visibleTickets()` now folds project filter + search); `type="search"` input at the section head.

**Condensed toggle:** hides card bodies (`ticket.body && !condensed`) to fit more on screen.

**Priority chip is clickable:** cycles low→medium→high (`cyclePriority`, PATCHes immediately).

**Full width:** dropped `max-width: 1200px` from `.content` in `+layout.scss` so the 6-lane board uses the whole monitor.

Files: `packages/shared/src/agent-dashboard.ts`, `apps/server/.../store.ts`, `apps/web/src/routes/agent-dashboard/+page.svelte` + `+page.scss`, `apps/web/src/routes/+layout.scss`. On branch off `refactor/shared-from-source` (PR #38 still open) → new PR for board UX.

## 12:30 — Gotcha: stacked PR on a non-`main` base got no CI + no merge gate (D-027)

PR #39 was based on #38's branch (`refactor/shared-from-source`) for a clean diff — but showed **no CI checks and no branch-protection warning**. Root cause: both are scoped to `main` only. `ci.yml` triggers on `pull_request: branches: [main]` (base must be `main`), and protection (`verify` + 1 approval) lives only on `main` — so a non-`main` base silently opts out of both. Fixed: #38 had merged, so retargeted #39 to `main` (`gh pr edit 39 --base main`) → clean board-only 8-file diff. Retarget alone didn't fire CI (`edited` event, not in default `opened`/`synchronize`/`reopened` set), so **close→reopen** forced a `reopened` event → `verify` ran + passed, `mergeState=BLOCKED` (review required) as expected. Rule captured in D-027: base PRs on `main` by default; stacking is opt-in-with-eyes-open.

## 14:25 — Priority → P0–P5 (nullable), status lock, duplicate, status field, priority legend (D-028)

Big board + data-model change (branch `feat/priority-levels`, stacked on the ticket-detail branch since it edits that route's priority styling too). All green: full `verify` (build + typecheck + eslint + 53 server / 16 web), 0 warnings.

**Priority model (D-028):** `low/medium/high` → **P0–P5**, plus **unset**. Domain/API uses `null` for unset (`AgentTicket.priority: TicketPriority | null`). DB column is `TEXT NOT NULL`, so unset is stored as the sentinel **`'none'`** and mapped at the store boundary (`fromDbPriority`/`toDbPriority`) — a real NULL would need a table rebuild, which would cascade-delete all child rows (relations/tags/events/reminders). Shared exports `PRIORITY_LABELS` + `PRIORITY_DESCRIPTIONS`.

**Data migration:** server `migrate()` step `agent_tickets_priority_to_p_levels` remaps in-place on boot (high→P1; medium+in_progress/completed→P3; medium+backlog→unset; low→P4; catch-all→P3), atomic with deploy — no manual API calls. Remapped `tickets.seed.json` to match (191 unset / 48 P3 / 7 P1). Data confirmed: prod had only medium(241)+high(19), no low, no assignees.

**Status field + lock:** create/edit modal gained a Status select (new tickets default backlog; edit defaults to current). Lock is **assignee-gated** (user's call): status editable unless `assignee != null && status ∈ {queued,in_progress,in_review,completed}` — so nothing locks today, activates when Sortie assigns. Locked → status select disabled + note, and PATCH omits status. Drag: `draggable={!isStatusLocked}`.

**Other UI:** priority chip is now an in-place native `<select>` (P0–P5 + None) replacing the cycle-on-click; duplicate button (⧉) → creates `[Duplicate] …` in backlog; info button (i) by the search bar opens a priority legend modal. Detail route updated for P-levels too.

Files: shared `agent-dashboard.ts`/`index.ts`; server `store.ts`/`routes.ts`/`schema.ts`/`seed/seed-tickets.ts`/`seed/tickets.seed.json`/`store.spec.ts`; web board `+page.svelte`/`+page.scss` + detail route. (generate.ts left as-is — defunct one-off, uses its own local Priority type.)

## 14:40 — Regenerated tickets.seed.json from prod (D-028 follow-up)

The seed had been the stale 246-entry TODO-derived baseline (only its priorities remapped). Regenerated it from a live prod fetch: 260 tickets, projectId→slug, priorities run through the same remap (→ 187 unset / 54 P3 / 19 P1), source preserved (245 seed:* + 15 manual). None skipped (all have valid projects). So a fresh re-seed now restores the current board, not the old baseline. Folded into PR #42. Verify green (53+16).

## 14:55 — Seed preserves display-ids + snapshot warning (D-028 follow-up)

Extended the seed schema to carry `displayId` so a restore reproduces exact ids (PD-42 stays PD-42) instead of renumbering. `CreateTicketInput.displayId` (optional) → `createTicket` inserts it verbatim and advances the project's `seq = MAX(seq, n)` so later auto-ids don't collide; API/UI creates omit it and auto-allocate as before. Regenerated `tickets.seed.json` from prod with displayId included (260 entries, all present, no dup ids). Added test (forced id preserved + next auto-id = PD-43). Also expanded the `seedTickets` doc comment into a warning: the seed is a point-in-time snapshot + restore-onto-empty tool, NOT a sync/merge over a live board (would lose since-created rows / collide on the unique display-id index). Verify green (54 server + 16 web). Folded into PR #42.
