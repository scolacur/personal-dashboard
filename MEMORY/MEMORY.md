# MEMORY — Personal Dashboard

One line per day. Load today's and yesterday's files at session start.

- [2026-06-30](2026-06-30.md) — CI/CD pipeline AUTHORED + DEPLOYED + PROVEN: verify-on-PR + GHCR build + Watchtower pull-deploy; app LIVE on NAS (PRs #9–#15). NAS facts: `docker-compose` v1, app on **port 8088** (gluetun owns 8080), DJ library at `/volume1/music/dj-library/tracks`, public GHCR, Dockerfile compiles better-sqlite3 (no musl prebuild) + copies pruned node_modules. Version stamp at `/api/health`. Compose changes need manual NAS `git pull`+`up`. **Big Sortie debugging session + watchdog/ask_human build.** Sortie exit-128 had TWO causes: (1) bare `git clone` into PERSISTENT non-empty workspace dir → `rm -rf` first (`669d36b`); (2) hooks run in restricted env where `export proxy` doesn't take for git → inline `git -c http.proxy=egress-proxy:3128` on all network cmds (`3f07151`). Also: #6/#8 were `max_sessions:3`-capped (manual `.sortie.db` run_history reset via official `alpine`+sqlite, NOT keinos). Built **watchdog** (`sortie:stuck`, queued>20m/in-progress>120m) + **ask_human** (`sortie:awaiting-human`) Actions + WORKFLOW.md prompt (`0115850`). **#6 SUCCEEDED → PR #17.** 🔴 **P1 bug found:** on `needs-human-review` exit Sortie cancels the worker context → kills `after_run` (no `scm.json`) + handoff label (`context canceled`) → PRs land label-less + review loop broken. **P1 fix MERGED (PR #19) + VERIFIED end-to-end on #22/PR #23 (all 3 items proven): agent does push/PR/scm.json/relabel in-turn, `after_run`→safety-net, `before_run` regenerates scm.json, watchdog `rescue-labels`; D-016.** Evening also: **3rd exit-128 cause** = `after_create` rm deletes its own CWD (`$SORTIE_WORKSPACE`) → `cd /home/sortie` first (`a247b9b`); **agent `npm ci` before verify** (`ed1ca4d`, was shipping unverified); **workspaces now EPHEMERAL** (`/tmp/sortie-workspaces`, no NAS bind-mount; `e9b2102`; rm still needed for within-run re-dispatch). 📌 PINNED: review-fix continuation agent lacked inline-comment file/line context (misimplemented feedback). Sortie config (`WORKFLOW.md`) bind-mounted, deploys via `sortie-refresh` (force-recreate; inode trap), NOT Watchtower. Tonight's commits pushed DIRECT to main via admin bypass. Pref: [[prefer-official-docker-images]].
- [2026-06-29](2026-06-29.md) — SCSS convention: styles always in own files (`<style lang="scss" src>`); needs svelte-preprocess (vitePreprocess ignores `src`) + sass; documented in PROJECT.md §5.
- [2026-06-28](2026-06-28.md) — Sortie-readiness: shipped ESLint/Prettier + vitest (6→41 tests) + hardened `verify` to `main` (PR #1 merged), normalized repo, authored pilot issue #2; only #6–7 (Sortie/NAS) left.
- [2026-06-27](2026-06-27.md) — Session start. Background: Symphony sibling project (D-013/D-014); Music Tracker steps 1–4 done.
- [2026-05-26](2026-05-26.md) — Session start. Background: Symphony sibling project (D-013/D-014); Music Tracker steps 1–4 done.
- [2026-05-25](2026-05-25.md) — Reconciled agent-dashboard PROJECT.md; D-013/D-014 added: Symphony standalone in multi-agent-linear-workflow/, Claude Code as runner, Mission Control UI in Dashboard consuming Symphony API.
- [2026-05-24](2026-05-24.md) — Session initialized. Project in design/planning phase, no code yet.
