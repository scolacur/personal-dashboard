# MEMORY — Personal Dashboard

One line per day. Load today's and yesterday's files at session start.

- [2026-07-01](2026-07-01.md) — **Agent Dashboard "Mission Control" cross-project ticket Kanban BUILT** (branch `feat/agent-dashboard-phase1`, 5 commits, NOT merged/deployed). Dashboard-owned data model: `agent_projects` (key PD/C/NSW) + `agent_tickets` (display_id `PD-7`, soft-delete `archived_at`, assignee, recur_interval) + relations/tags/events/reminders (schema reserved; UIs deferred to follow-up tickets). **Non-destructive migration framework** (`migrate.ts`, `_migrations` ledger, addColumn — D-021; "schema only grows", Steve's data-safety req). **todo→ticket rename** everywhere. **Seed: 246 tickets** parsed from 24 `TODO.md` + core `META-TODOS.md` (`seed/generate.ts`→committed `tickets.seed.json`; idempotent `import.ts` on source+title; 49 completed; PD `[Domain]` prefixes, core unprefixed) — dev DB (`apps/server/data/dashboard.db`) seeded, live at `/agent-dashboard`:8080. **Archived** 24 PD TODO.md → `/Users/steve/Documents/Dev/archive/TODO-<domain>.md` + repointed CLAUDE/README/PROJECT; **core META-TODOS NOT archived** (load-bearing, 62 refs) → high-pri core ticket to read from dashboard API. 5 statuses: Backlog/Ready manual, In progress/In review/Completed **derived from GitHub labels** (not Sortie :7678). 52 tests. **Next: merge/deploy + Phase 3 convert-to-Sortie-issue** (needs ANTHROPIC_API_KEY + write GH token on NAS). Also added 2 HIGH infra tickets: SQLite `.backup` cron (Hyper Backup→Backblaze not SQLite-aware) + reverse proxy. Dev gotcha: web reads `@dashboard/shared` from `dist/` → rebuild shared after edit (dev alias removed on merge; main D-019 made shared ESM).
- [2026-06-30](2026-06-30.md) — CI/CD pipeline AUTHORED + DEPLOYED + PROVEN: verify-on-PR + GHCR build + Watchtower pull-deploy; app LIVE on NAS (PRs #9–#15). NAS facts: `docker-compose` v1, app on **port 8088** (gluetun owns 8080), DJ library at `/volume1/music/dj-library/tracks`, public GHCR, Dockerfile compiles better-sqlite3 (no musl prebuild) + copies pruned node_modules. Version stamp at `/api/health`. Compose changes need manual NAS `git pull`+`up`. **Big Sortie debugging session + watchdog/ask_human build.** Sortie exit-128 had TWO causes: (1) bare `git clone` into PERSISTENT non-empty workspace dir → `rm -rf` first (`669d36b`); (2) hooks run in restricted env where `export proxy` doesn't take for git → inline `git -c http.proxy=egress-proxy:3128` on all network cmds (`3f07151`). Also: #6/#8 were `max_sessions:3`-capped (manual `.sortie.db` run_history reset via official `alpine`+sqlite, NOT keinos). Built **watchdog** (`sortie:stuck`, queued>20m/in-progress>120m) + **ask_human** (`sortie:awaiting-human`) Actions + WORKFLOW.md prompt (`0115850`). **#6 SUCCEEDED → PR #17.** 🔴 **P1 bug found:** on `needs-human-review` exit Sortie cancels the worker context → kills `after_run` (no `scm.json`) + handoff label (`context canceled`) → PRs land label-less + review loop broken. **P1 fix MERGED (PR #19) + VERIFIED end-to-end on #22/PR #23 (all 3 items proven): agent does push/PR/scm.json/relabel in-turn, `after_run`→safety-net, `before_run` regenerates scm.json, watchdog `rescue-labels`; D-016.** Evening also: **3rd exit-128 cause** = `after_create` rm deletes its own CWD (`$SORTIE_WORKSPACE`) → `cd /home/sortie` first (`a247b9b`); **agent `npm ci` before verify** (`ed1ca4d`, was shipping unverified); **workspaces now EPHEMERAL** (`/tmp/sortie-workspaces`, no NAS bind-mount; `e9b2102`; rm still needed for within-run re-dispatch). **Review-fix continuation was BROKEN → FIXED + VERIFIED (D-017, `3fa4c2e`/`d3010a4`):** root cause = review-feedback prompt gated behind `{{ if .run.is_continuation }}`, which is FALSE for review-reaction dispatches (confirmed via transcripts: banner 0×, feedback 0×) → agent re-ran from scratch. Fix = unconditional STEP 0 (detect existing PR → fetch `gh api .../reviews` summary body + inline + own diff → EDIT not append); also tests-required + descriptive PR/commit titles. VERIFIED on #26/PR #27 (summary review → agent edited the fn + test, `/reviews:3` feedback:16×). 📌 NEW open: `packages/shared` has no vitest config (logic tested indirectly from `apps/server/src`). Sortie config (`WORKFLOW.md`) bind-mounted, deploys via `sortie-refresh` (force-recreate; inode trap), NOT Watchtower. Tonight's commits pushed DIRECT to main via admin bypass. Agent tool-call transcripts: `$HOME/.claude/projects/<encoded-cwd>/<session>.jsonl` (great debug handle). Pref: [[prefer-official-docker-images]]. **ALSO (later session): TODO→Sortie pipeline Phase 1 BUILT+VERIFIED** — Agent Dashboard Kanban at `/agent-dashboard`; new dashboard-owned `agent_todos` table (**D-018**; distinct from the D-014 agent-run tables — this is Steve's backlog; derived statuses come from GitHub labels not Sortie's internal API); backend widget + shared types + 5-col Kanban UI (Add/move/edit/delete); `npm run verify` green + all CRUD curled; UNCOMMITTED. 3-phase plan at `~/.claude/plans/mellow-coalescing-emerson.md` (P2 = seed-import then DELETE the repo TODO.md files; P3 = Claude-API "convert to Sortie issue" draft-then-submit). **DEV GOTCHA:** web imports `@dashboard/shared` from `dist/` (not `src/`) → edits need shared rebuild + Vite restart; fixed with a **dev-only Vite alias** to `src` (`command==='serve'` in `apps/web/vite.config.ts`; prod build/svelte-check still use `dist/`).
- [2026-06-29](2026-06-29.md) — SCSS convention: styles always in own files (`<style lang="scss" src>`); needs svelte-preprocess (vitePreprocess ignores `src`) + sass; documented in PROJECT.md §5.
- [2026-06-28](2026-06-28.md) — Sortie-readiness: shipped ESLint/Prettier + vitest (6→41 tests) + hardened `verify` to `main` (PR #1 merged), normalized repo, authored pilot issue #2; only #6–7 (Sortie/NAS) left.
- [2026-06-27](2026-06-27.md) — Session start. Background: Symphony sibling project (D-013/D-014); Music Tracker steps 1–4 done.
- [2026-05-26](2026-05-26.md) — Session start. Background: Symphony sibling project (D-013/D-014); Music Tracker steps 1–4 done.
- [2026-05-25](2026-05-25.md) — Reconciled agent-dashboard PROJECT.md; D-013/D-014 added: Symphony standalone in multi-agent-linear-workflow/, Claude Code as runner, Mission Control UI in Dashboard consuming Symphony API.
- [2026-05-24](2026-05-24.md) — Session initialized. Project in design/planning phase, no code yet.
