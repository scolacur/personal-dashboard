# MEMORY — Personal Dashboard

One line per day. Load today's and yesterday's files at session start.

**Index rule:** one line per day, a single sentence, ≤ ~200 chars, navigational only — points at the day file, never summarizes it. Detail lives in the day file; durable decisions in `DECISIONS.md`. (Governed by `~/.claude/commands/wrap-up.md` Step 5.)

- [2026-07-17](2026-07-17.md) — Actioned both audit runs (D-045, non-cumulative → union); PD-377 mis-flagged-Epic fix; grilled lane/epic/refine knot → **D-058** queue-model, sliced PD-397–400; then **finished the Robot epic (D-055)** — shipped **C6 cutover** (#252 — DB-authoritative, github-sync + issue-minting retired, DB-native completion) + **C7** (#256 — sortie→robot sweep + `robot_enabled` migration + ops/sortie removed), **cutover LIVE**, + PD-393/394 UX; filed PD-391/395/396; split queue-model into its own epic, closed PD-341. Then **PD-377 troubleshoot**: live run hit the 50-turn cap → decomposed into Epic + PD-402/403/404 (blocks chain) + made Refine turn-budget-aware (PR #258). Also shipped+deployed **PD-397** (#259 — D-058 queue-model core, proven live via PD-402), fixed the **D-058 number collision** (#257 — epic-resize→D-059), and filed PD-410 (nav killswitch). **Next session (pd-401):** closed the PD-397 board-status loose end (deployed live but still `prioritized`→completed) + shipped **PD-398** (D-058 slice B — Epic **Populate** mode: `approveRefine` reinterprets a decompose-on-Epic as members via `epic_id`, Epic stays open, no split; fixes PD-382; verify green, **PR #260 MERGED**). Then repo hygiene: landed the leftover 07-17 D-035 Mac-Mini-migration doc work (DECISIONS.md + README, **PR #261**) and committed the accumulated `MEMORY/` pile straight to main (aging-pass renames + day files 07-08→07-17). Then shipped **PD-399** (D-058 slice C — board queue UX: table-wide assignee filter + not-Ready bypass confirm modal on drag/edit + "needs shaping"/"⚠ bypassed" card badges; most of the slice was already seeded by PD-397's merge; verify green, **PR #262**, web-only auto-deploys). Then **PD-400 (D)** — clean agent-session teardown on manual close/complete (PD-392 fix: `updateTicket` clears agent_state + resolves needs/awaiting-human notifications + logs `robot_session_ended`; terminal tickets derive refineState=null; MANUAL path only, loop's own completion untouched; verify green, committed `dcd2c11`, web-app auto-deploy). **D-058 epic PD-401 now A–D all done.** Also **re-grilled Mac Mini migration PD-188 step 3** (post-D-055): `/data`=Colima VM-native (preserves uid-split/D-039), stay Colima, disarmed→verify→arm; rewrote PD-188 body + amended D-035 (Sortie→agent-worker); LOE ~8–16h hands-on.
- [2026-07-16](2026-07-16.md) — Robot loop epic (D-055): C1 prove-on-one PASSED, then shipped C2 fault-guardrail (#242), C3 observability (#243), C4 remediation + PD-374 redesign (#245), C5 fold four sortie-*.yml bridges into the loop (#251); + D-057 refine-approve-never-dispatches (#247); Sortie container stopped. Next: C6/PD-347 cutover (+ retire issue-minting).

*(Older days archived — see [archive/INDEX.md](archive/INDEX.md).)*
